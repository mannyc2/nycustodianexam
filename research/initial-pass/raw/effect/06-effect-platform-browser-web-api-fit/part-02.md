| **fetch / HTTP client**        | `fetch`, `Request`, and `Response` are appropriate for browser resource handling, Service Worker dispatch, and very small one-off requests.       | `ContentPackSource`, `RemoteContent`, and later `WorkerApiClient`.                                                       | Stable v3 `HttpClient` plus `FetchHttpClient.layer`; `HttpApiClient` only if an owned API contract later justifies it. `HttpClient` supplies status filters, retries, request transforms, typed errors, and Schema response decoding. ([Effect TS][10]) | **Use Effect `HttpClient` behind project services for application-owned HTTP. Use native fetch elsewhere.**                                                      | Support mature; portability high; testing high; bundle medium; semantic mismatch low; needed now for versioned content-pack acquisition.               |
| **URL / URLPattern**           | Use `URL` and `URLSearchParams` directly. Use `URLPattern` only where the browser target supports it or after feature detection.                  | Usually none. Pure content-location or route helpers may be ordinary functions.                                          | `@effect/platform/Url` and `UrlParams` are thin safe and immutable helpers around native URL values, not a runtime service.                                                                                                                             | **Use native values directly. No Layer.** Optional Effect URL helpers are an ergonomic choice.                                                                   | Portability already high; testing is pure; bundle none; service mismatch high; needed now, but not as a capability service.                            |
| **cookies**                    | Browser cookie handling is managed by the browser. Cookie Store is newer; `document.cookie` is synchronous. Worker-side cookies are HTTP headers. | Add a `Session` or `SessionCookie` service only if account-backed authentication is introduced.                          | Effect `Cookies` is primarily an HTTP cookie model and parser, not a browser cookie-store abstraction.                                                                                                                                                  | **Do not add a browser cookie service now.** Use Effect cookie parsing only at a future Worker HTTP boundary.                                                    | Portability low; test benefit currently none; bundle avoidable; semantic mismatch high; no current product need because accounts are not required.     |
| **headers**                    | Use native `Headers` at Fetch, Service Worker, and Worker boundaries.                                                                             | None unless the application has a domain-specific signing or authorization policy.                                       | Use Effect `Headers` inside Effect HTTP workflows.                                                                                                                                                                                                      | **Do not wrap Headers independently. Keep each representation within its owning HTTP boundary.**                                                                 | Mature; pure structured data; test benefit low; bundle none outside HTTP; needed only as part of networking.                                           |
| **file / blob handling**       | Use `File`, `Blob`, `FormData`, `arrayBuffer()`, `text()`, and `stream()` directly.                                                               | Introduce `PackImportExport` or `PacketExport` only when file import/export becomes a feature.                           | Effect HTTP understands Blob/FormData-style request bodies, but Effect `FileSystem` is an OS-style path filesystem, not browser File/Blob handling.                                                                                                     | **Use native File/Blob inside a project import/export adapter. Never provide Platform FileSystem to browser domain code.**                                       | Support mature; portability medium through project service; test benefit medium; bundle low; Platform FileSystem mismatch very high; need conditional. |
| **streams**                    | Use Web Streams at Fetch, Blob, Compression, and Service Worker boundaries.                                                                       | A project service should expose `Effect.Stream` only when streaming is part of its meaningful contract.                  | Core `Stream` provides Effect composition, scopes, backpressure, interruption, and testing. Stable BrowserStream only converts window/document events into Streams. ([Effect TS][11])                                                                   | **Keep native streams at Web boundaries; convert once if domain processing benefits from Effect Stream. Avoid repeated conversion.**                             | Native support mature; portability medium; testing medium-high; bundle medium; semantic mismatch medium; probably not needed until packs are large.    |
| **WebSocket**                  | Native `WebSocket` is sufficient for a single simple channel, though the classic API lacks backpressure. ([MDN Web Docs][12])                     | `RealtimeChannel` if a real-time feature is ever introduced.                                                             | Stable `Socket` and `BrowserSocket` provide scoped lifecycle and Effect integration.                                                                                                                                                                    | **Do not include now. If later required, use BrowserSocket behind a project service.**                                                                           | Portability and testing become high only once the feature exists; bundle medium; current need none.                                                    |
| **crypto / hash**              | Use Web Crypto `crypto.subtle.digest`, `getRandomValues`, and `randomUUID`.                                                                       | `ContentDigest` or `IntegrityVerifier` for content identity, pack manifests, and known-algorithm policy.                 | Stable v3 browser package has no browser crypto service. V4 adds `BrowserCrypto`, but it is prerelease.                                                                                                                                                 | **On v3, use a project-owned service backed by native Web Crypto.** Share that service with Node and Worker implementations.                                     | Support mature; portability high; testing high through known vectors; bundle low; mismatch low; likely needed for immutable content verification.      |
| **random values**              | Web Crypto random values are for security identifiers. Do not use `Math.random()` where reproducibility matters.                                  | A domain-specific seed policy may be project-owned, but the PRNG itself does not need another wrapper.                   | Core Effect `Random` supports seeded deterministic sequences and fixed test sequences. It is explicitly pseudo-random, not cryptographic.                                                                                                               | **Use Effect `Random` for question ordering and deterministic simulations. Use Web Crypto only for security/unique identifiers.**                                | Portability high; testing very high; bundle low; mismatch low; needed now.                                                                             |
| **clocks**                     | Use `Date` for interoperable timestamps and `performance.now()` only for browser performance measurements.                                        | Introduce domain types such as `AttemptStartedAt` or `ReviewDueAt`, not a redundant clock wrapper.                       | Core Effect `Clock` provides current time and sleep; `TestClock` gives deterministic virtual time.                                                                                                                                                      | **Use Effect Clock for use cases, retries, timeouts, review timing, and tests.**                                                                                 | Portability high; testing very high; bundle low; mismatch low; needed now.                                                                             |
| **browser storage**            | `localStorage` and `sessionStorage` are synchronous and suitable only for small values. ([MDN Web Docs][13])                                      | `Preferences`, exposing typed preference operations rather than arbitrary keys.                                          | Stable `BrowserKeyValueStore` provides only localStorage and sessionStorage Layers. ([Effect TS][14])                                                                                                                                                   | **Use KVS only underneath a typed Preferences service.** Direct localStorage underneath the same service is also acceptable if it produces a smaller result.     | Portability medium; testing high; bundle low; semantic fit good for preferences; needed now or soon.                                                   |
