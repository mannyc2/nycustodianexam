# bun.lock status

A real `bun.lock` is intentionally not fabricated.

This lane requires a private Bun fixture, but the available execution environment did not provide a usable Bun/package-install path for running `bun install` at the selected exact cohort. Therefore package installation, installed `node_modules/effect/AGENTS.md` review, lockfile generation, typecheck, and execution of this fixture remain BLOCKED.

The committed `package.json` records the intended exact coordinates for later execution.
