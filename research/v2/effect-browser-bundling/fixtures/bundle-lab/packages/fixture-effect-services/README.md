# Effect service package — source emission blocked

This package owns the shared Effect services for the browser question-player probes. Its explicit dependencies are pinned, but its Effect source is intentionally withheld until the exact Bun install succeeds and the installed `node_modules/effect/AGENTS.md` has been read as required by the shared contract.
