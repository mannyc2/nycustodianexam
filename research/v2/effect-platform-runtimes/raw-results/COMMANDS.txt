# R2.3 probe commands

# Browser and Service Worker runtime probe
# Chromium carried a managed URLBlocklist ["*"]. The exact policy JSON was
# backed up, only URLBlocklist was removed for the isolated localhost run, and
# the original file was restored byte-for-byte immediately afterward.
python3 fixtures/runtime-matrix/browser_server.py \
  --directory fixtures/runtime-matrix/browser \
  --event-log raw-results/browser-server-events.jsonl \
  --port 8766
/opt/pyvenv/bin/python fixtures/runtime-matrix/run_browser_probe.py \
  --url http://127.0.0.1:8766/index.html \
  --out raw-results/browser-runtime-probe.json \
  > raw-results/browser-runtime-probe.stdout.txt \
  2> raw-results/browser-runtime-probe.stderr.txt

# Cloudflare-compatible TypeScript/Web handler shape
cd fixtures/runtime-matrix/workerd
npx --no-install tsc -p tsconfig.json --pretty false
node run-probe.mjs
wc -c worker.ts dist/worker.js dist/worker.d.ts dist/worker.js.map

# Environment and blocker probes
chromium --version
node --version
npm --version
npx --no-install tsc --version
command -v bun workerd wrangler miniflare docker podman gh
timeout 8 curl -fsSI https://registry.npmjs.org/effect
npm cache ls 'effect@4.0.0-rc.110'

# Integrity
find . -type f ! -name MANIFEST.sha256 -print0 | sort -z | xargs -0 sha256sum > MANIFEST.sha256
