# Effect probe sources

These sources capture the exact intended executable gates for the pinned `4.0.0-rc.110` cohort. They were not executed or type-checked because the container could not install the declared packages and had no Bun executable.

They must not be cited as runtime observations. Their value is to make the unresolved gates mechanically specific for a later connected environment.
