# Recovered SVG example deliverables

**Source form:** exact generated/downloadable files recovered from the ChatGPT Library.

The full conversation transcript and the assistant's separate completion message were not available. Nine standalone SVG source files are preserved byte-for-byte under `FILES/original/`.

The separately materialized PNG previews, contact sheet, and `ny-custodian-svg-examples.zip` were inspected and checksummed but are not committed here: they are derivative/render archives, while their source SVGs are preserved. Their exact metadata is recorded in the orchestration file ledger.
