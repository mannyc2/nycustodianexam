# Plans 004/005 unified fieldwork operations packet

**Packet version:** `FW-004-005-v2` (coordinator-review amendment).

**Status:** `NEEDS ATTENTION — external participant resources`. **No outreach,
pilot, participant exposure, or product decision is authorized by this record
alone.** Step 1 is released at the exact coordinate below. Material fieldwork
resources remain absent, no participant session has occurred, and the preserved
Plan 004/005 execution artifacts still need the phase-specific repairs and
approvals below.

This packet reconciles the consumer-language protocol in Plan 004 with the
learner-task/navigation protocol in Plan 005. It is an execution overlay, not a
replacement for either reviewed plan and not participant evidence. Exam truth
and scope remain controlled by `docs/`; product behavior and implementation
constraints remain controlled by `product/`. A conflict with those authorities
is a stop, not a research hypothesis.

## 1. Immutable coordinates and truthful starting state

Observed 2026-08-28:

| Coordinate | Value |
|---|---|
| Planning coordinate | `e6f911901f7f18f6716204309fee8b103419a5e0` |
| Released Step 1 `origin/main` coordinate | `9fc7dcacfc961752e5d9a2cedbc426deead54a05` |
| Merged preservation/status PR | [#40](https://github.com/mannyc2/nycustodianexam/pull/40) |
| Coordinator release | `STEP 1 LANDED` for `9fc7dcacfc961752e5d9a2cedbc426deead54a05` |
| Durable recovery package | `recovery/plan-004-consumer-language-prototypes/` |
| Recovery manifest | `recovery/plan-004-consumer-language-prototypes/recovery-manifest.json` |
| This preparation branch | `codex/uiux-orchestration-02-fieldwork` |
| Plan 004 draft PR/head | [#37](https://github.com/mannyc2/nycustodianexam/pull/37), `fecc71c5ea240385b3d98f896b1152022a2bbbe8` |
| Plan 005 draft PR/head | [#38](https://github.com/mannyc2/nycustodianexam/pull/38), `9daddbfde073f1f73d806a68dac427b69efc8359` |
| Decision owner recorded by both charters | `mannyc2` |

PR #40 and the coordinator's release establish the Step 1 gate. The durable
recovery authority is its `README.md`, `recovery-manifest.json`, and exact eight
files under `prototypes/`. The package preserves inputs; it is not a formal
round manifest, an exposure approval, participant evidence, or an accepted
language direction.

The user's approval authorizes in-scope repository work, research operations,
branches, pushes, PRs, and verification. It does not create a participant pool,
a way to contact that pool, money or a payment rail, a private research-data
system, research devices, or a prototype exposure endpoint. It also does not
turn executor prose into the structured approval comments required by the
existing retained verifiers.

### Evidence and sample accounting

| Workstream | Preserved work | Human evidence | Decision state |
|---|---|---|---|
| Plan 004 | 526-document/27-route baseline; 120 copy families; 79 proposed rewrite families; 24 keep; 9 move to details; 5 internal only; 2 content re-review; 1 support relocation; 847 dynamic-message anchors; 13 dated benchmark records; eight editable `CL-1` prototype files | Pilot `n=0`; R1 `n=0`; R2 `n=0`; observations `n=0`; issue rows `n=0` | No direction selected; `product/CONTENT_DESIGN.md` correctly absent |
| Plan 005 | 21 destination families; 32 unique core route IDs; four acquisition spokes; 21 learner-task statements | Open sort `n=0`; threshold pilot `n=0`; tree test `n=0`; first-click R1 `n=0`; first-click R2 `n=0`; distinct access-strategy people `n=0` | No candidates, thresholds, locks, selected model, or product-contract promotion |

The eight Plan 004 working files are durably preserved byte-for-byte under
`recovery/plan-004-consumer-language-prototypes/prototypes/`. The recovery
manifest records their original observation provenance. Their preserved
raw-byte SHA-256 values are:

| Prototype | SHA-256 |
|---|---|
| `hazard-feedback.html` | `c9441b28584f1453b8afe982f61c906a9d5f98e352fcc886b1e135e64b3e495b` |
| `home.html` | `05df1d5bdc797137d0b35e0a30eaf1b2781351e542b0b4d7a2c68da42d8231b2` |
| `offline-data.html` | `0eede6bbc7d6c7f2a2b1434eb7f2ad1fa3929c0ecf3bc9fe823151a558d42a8f` |
| `practice-start.html` | `cfe277890d2c6ef9490c74055c6ba94a5921a0a6c465aaf34b3b960a07a4e06e` |
| `profile.html` | `e672f50871d80572cdc2e6f13e3fe85e04a8c5ac186f0228093ce85e8fd79ca6` |
| `question-feedback.html` | `10336e7ce3d85b587b0eea5f8e8eb6d717e1a595775e11256201f62b45c676db` |
| `review.html` | `207ff61103d67bdd5dd5be8d4ef154cb804aa767f2583ea57698525f664c2c67` |
| `trust-recovery.html` | `36f1cec7646ccddf7e5cd525b88517cf7372cfc5eb187beae7332bac4eafa2f4` |

The complete preserved set digest is
`f1ef0a2dec44ae04c8c2b3e8f94fe9e59b3c38a54d3310ae50b4b7dde10ecf14`.
These hashes identify reusable draft inputs, not approved exposure artifacts.
The recovery manifest truthfully records that both formal snapshot rounds were
empty and no formal prototype manifest existed.

## 2. Staged go/no-go ledgers

Readiness is cumulative but action-specific. A later gate cannot block an
earlier lawful action whose purpose is to make the later gate possible.
`PRE-RECRUITMENT` permits outreach, screening, and scheduling only; it permits
no study exposure. `PRE-DISCOVERY/PILOT` permits only the named uncounted or
exploratory phase. `PRE-FORMAL-EXPOSURE` is fail-closed for each formal round
and exact artifact. A later failure suspends that later action, not already
consented contact administration.

### 2.1 `PRE-RECRUITMENT` — contact, screening, and scheduling only

| Gate | Current state | Required evidence |
|---|---|---|
| Step 1 preservation/status commit | `MERGED` | PR #40 / `9fc7dcacfc961752e5d9a2cedbc426deead54a05` |
| Coordinator Step 1 release | `RELEASED` | Coordinator message naming `9fc7dcacfc961752e5d9a2cedbc426deead54a05` |
| Plan 004 preservation | `RECOVERY LANDED; RESEARCH DRAFT` | PR #40 recovery plus #37 findings, with zero samples and pending decision unchanged |
| Plan 005 preservation | `STATUS LANDED; RESEARCH DRAFT` | PR #40 status plus #38 inventory, without promoting hypotheses |
| Recruitment/contact channel | `ABSENT` | Named real channel, authorized contact owner, exact target pool, workflow, and availability window |
| Real target-user access | `ABSENT` | Proof that the channel can reach qualifying real people; no names in Git |
| Paid or unpaid terms | `ABSENT` | Exact phase terms; if paid, amount, funding, delivery, timing, and cancellation; if unpaid, explicit no-payment terms |
| Contact/payment registry | `ABSENT` | First access-separated private system in section 4, including contact consent for scheduling, payment when used, withdrawal, and deletion requests |
| Research evidence store and screener writer | `ABSENT` | Second access-separated private system; implemented `fieldwork-screener-v1`, `fieldwork-consent-attrition-v2`, and strict private validator; the store is ready to receive de-identified rows keyed by `recruitment_key` before screening |
| Screener/contact materials | `PACKET READY; INSTANTIATION ABSENT` | Exact channel copy, screener version/hash, contact consent, retention/deletion language, cohort-reuse token disclosure/check, and per-phase compensation-terms hash |
| Pre-recruitment structured approvals | `ABSENT` | Owner-authored schema-amendment and operations/sample comments on both #37 and #38; no formal exposure approval is required yet |
| Incident owner | `ABSENT` | Named human contact and response path for privacy, safety, accessibility, and protocol incidents |
| Recruitment recording | `READY: false` | Recruitment calls/messages are not recorded; a change needs separate approval and disclosure |

Every row in this table must be ready before first contact. Passing it creates
screened or scheduled people, not participants and not evidence. A draft PR URL
is an approval channel, not a recruitment channel.

### 2.2 `PRE-DISCOVERY/PILOT` — phase-specific exploratory exposure

The common rows below and the relevant phase row must be ready before that
phase begins. Formal snapshots and formal-round exposure comments are
deliberately not prerequisites here.

| Gate | Current state | Required evidence |
|---|---|---|
| `PRE-RECRUITMENT` | `FAIL` | Every section 2.1 row ready and the person screened eligible |
| Screening/consent infrastructure | `INHERITED FROM 2.1` | Approved research store plus strict screener, shared attrition, and phase-terms validators remain operational |
| Human roles | `ABSENT` | Named moderator, incident owner, and second human reviewer |
| Session facility | `ABSENT` | Required device/browser/AT and private exposure method; recording remains false by default |
| Plan 004 uncounted pilot | `NOT READY` | Authority-repaired working copies; isolated direction/role/state; working-artifact hashes; pilot guide; one screened real person; no `R1-P*` ID |
| Plan 005 open sort | `NOT READY` | Exact 24-card/script hashes, private open-sort schemas, no candidate IA/language exposure, screened real people |
| Plan 005 threshold pilot | `NOT READY` | Completed open-sort aggregates and hash-bound formation record; pilot A/B artifacts and schedule hashes; private threshold schemas; 2–3 fresh people eligible for the pilot aggregate and barred from formal reuse |

The Plan 004 pilot may cause prototype or script edits. Plan 005 open sort may
cause candidate formation. The threshold pilot may cause task, scoring,
candidate, or threshold changes. Every exposed exploratory byte is still
hashed and recorded, but none is mislabeled as a formal immutable snapshot.
Any hierarchy, task, scoring, or artifact change after the threshold pilot
requires a new threshold-pilot version before formal exposure.

### 2.3 `PRE-FORMAL-EXPOSURE` — exact round and artifact

Every common row plus the intended plan's rows must be ready before activating
a formal study ID, sending a formal-session link, or exposing a formal task.
Rows for the other plan are `N/A`, not hidden prerequisites.

Common formal rows:

| Gate | Current state | Required evidence |
|---|---|---|
| Cohort independence | `FAIL` | Private reuse check passes the section 5 matrix before formal ID issuance |
| Formal exposure facility | `ABSENT` | Exact device/browser/AT/private host or local method and time-bounded access boundary |
| Recording | `READY: false` | Default no recording; any true value needs separate opt-in, tool, viewers, storage, retention, and deletion approval |
| Exact-round structured approval | `ABSENT` | Owner-authored formal exposure comment on the relevant #37 or #38, bound to exact bytes/schedule/access method |
| Human analysis | `ABSENT` | Named moderator, incident owner, and second human critical/high reviewer |

Plan 004 formal rows:

| Gate | Current state | Required evidence |
|---|---|---|
| Relevant Plan 004 pilot | `NOT RUN` | Valid uncounted pilot under section 2.2 |
| Plan 004 formal artifact safety | `FAIL` | Post-pilot repair; immutable R1/R2 source snapshot and exposure manifest; exact hashes; authority/security/accessibility review |
| Plan 004 evidence verifier | `FAIL` | v2 observation plus shared attrition/exposure joins, random order, independent control scoring, eligibility derivation, and tamper tests |

Plan 005 formal rows:

| Gate | Current state | Required evidence |
|---|---|---|
| Relevant Plan 005 discovery/pilot | `NOT RUN` | Valid open-sort, candidate formation, and threshold pilot under section 2.2 |
| Plan 005 formal candidates | `FAIL` | Two immutable distinct hierarchy candidates, exact artifact/hierarchy hashes, declared eight thresholds, and locked task/target/schedule files |
| Plan 005 evidence verifier | `FAIL` | Shared attrition plus exploratory/formal schemas, deterministic aggregates, all phase gates, artifact/issue joins, and tamper tests |

The exact-round prerequisite matrix below is also mandatory. Only the requested
round's row applies; a later round cannot inherit an earlier approval or hide a
missing revalidation.

| Exact round | Additional prerequisite before its first study ID |
|---|---|
| Plan 004 R1 | Valid uncounted pilot; repaired immutable R1 source/exposure manifests; frozen R1 allocation plus reserves; R1 verifier; owner R1 exposure comment |
| Plan 004 R2 | Accepted valid R1 evidence closure; every R1 critical family in an approved R2 retest plan; immutable revised R2 bytes; frozen new-person R2 allocation plus reserves; R2 verifier; owner R2 exposure comment |
| Plan 005 tree test | Valid open-sort closure, formation record, threshold pilot, eight declarations, 26-row tree schedule, frozen balanced candidate allocation plus reserves, tree verifier, owner tree exposure comment |
| Plan 005 first-click R1 | Same locked candidates/tasks/thresholds; valid tree closure for both candidates; exact 26-row entry schedule; separate balanced allocation plus reserves; R1 verifier; owner R1 exposure comment |
| Plan 005 first-click R2 | Valid tree and R1 closures; progression record proves one candidate passed all 13+13 gates and both critical gates; accepted Plan 004 language contract on `main`; unchanged hierarchy plus exact 13-row reconciled schedule; new-person allocation plus reserves; R2 verifier; owner R2 exposure comment |

Failing any row blocks only the named formal round. It never licenses a mutable
artifact, backfilled order, relaxed threshold, reused exposed cohort, or
simulated participant.

## 3. Preflight repair without restarting completed inventory work

### 3.1 Preserve, do not repeat

- Retain Plan 004's 120-family classification, benchmark ledger, and dynamic
  message trace. Do not rerun them to create a new count.
- Retain Plan 005's exact 21-family/32-route/four-spoke inventory. Its priority,
  archetype, and shell fields remain explicitly unvalidated hypotheses.
- Preserve the current zero-sample aggregates. Never create placeholder
  participants or synthetic result rows.
- Reconcile the two draft heads named above against released Step 1 without
  force-pushing or losing either truthful zero-sample status row.

### 3.2 Repair Plan 004 prototypes before the uncounted pilot

Copy the eight identified draft inputs from the durable recovery package into a
fresh Plan 004 work root. Do not edit the preserved recovery bytes or call the
copied bytes R1. Apply all of these authority repairs before a human sees them:

1. Remove invented 10/20/40-question choices and five-minute estimates. Do not
   turn the implemented, site-designed 45/60/90 simulation lengths into claims
   about official exam length or timing.
2. Describe accepted hazard assets as original monochrome line illustrations,
   not photographs.
3. Use the correct fact state at the correct profile layer. Statewide series
   facts that do not apply are not `not_published`; do not claim all New York
   administrations are compatible or that the corpus completely covers an
   exam.
4. Replace the fabricated pipe-wrench guide citation with the exact maintained
   source/claim binding. No prototype may invent a publisher, page, section, or
   verification coordinate.
5. Use a reviewed, source-backed hazard scene and reviewed claims. Do not
   improvise compliance rules or a new scene for research convenience.
6. Remove the invented 12 MB pack size and any hard-coded source count that can
   drift from the mixed source registry.
7. Narrow privacy copy to the maintained local-first contract. Do not promise
   that every action always stays on-device.
8. Replace the unsupported claim that nobody outside the state has real exam
   questions with the project truth: this project does not possess, solicit,
   or publish secure or recalled exam material.
9. Make every editable working file writable by its owner. Freeze permissions
   only on a complete post-pilot snapshot.
10. Reconcile the working benchmark/ledger so successful later observations do
    not remain listed as unretrieved and the execution-base history is exact.

Each prototype must then expose exactly one assigned direction and one assigned
state at a time, with usable native controls for the task being observed. A
participant must be able to take a first action, open source detail, acknowledge
review, inspect a failed offline update, inspect an import preview, and reach a
reset confirmation. Rendering current, CL-D1, CL-D2, every state, and all
advanced detail together is not a valid exposure method.

CL-D3 has no surviving prototype and is formally `rejected-untested` for
`FW-004-005-v2`. It is not an eligible exposure or decision ID. It may return
only in a prospective v3 approved before contact with equivalent pilot,
isolated manifests, 6-or-8-person rounds, nine tasks, randomized controls,
critical retest, and owner gates. Its existence in desk hypotheses is not
participant evidence. `current` remains a diagnostic condition, never a
selectable direction.

### 3.3 Complete the retained verifiers at the gate that needs them

Schema parsing, consent/attrition handling, and pilot validation must exist
before exploratory exposure. Formal eligibility and decision gates must exist
before formal IDs. This avoids making post-pilot facts prerequisites for the
pilot while preserving fail-closed formal evidence.

The shared private boundary uses `fieldwork-compensation-terms-v1`,
`fieldwork-consent-attrition-v2`, `fieldwork-attrition-projection-v1`,
`fieldwork-screener-v1`,
`fieldwork-schema-migration-v1`, `fieldwork-phase-input-manifest-v1`, and
`fieldwork-phase-evidence-manifest-v1`. For Plan 004, retain
`consumer-language-prototypes-v1` as the exact eight-source manifest and add
`consumer-language-allocation-v1`, `consumer-language-participants-v2`,
`consumer-language-exposures-v2`, `consumer-language-pilot-v1`,
`consumer-language-task-observations-v2`, `consumer-language-issues-v2`, and
`consumer-language-aggregate-v2`. The validator must:

- add `pilot`, which verifies a consented real pilot and the exact safe working
  artifact hashes without adding evidence or requiring an R1 freeze;
- replace the old `--observations` input after amendment with required
  `--participants`, `--task-observations`, `--exposure-manifest`,
  `--allocation-schedule`, and `--attrition-ledger` inputs;
- require formal observations to join consent, allocation, assigned direction,
  task state/variant, independently scored current/candidate condition, exact
  isolated artifact, source prototype/manifest, access strategy, withdrawal,
  exclusion, and issue records;
- bind each #37 formal exposure approval to both the source prototype manifest
  and isolated exposure-set version/hash;
- derive eligibility and every aggregate rather than trusting entered counts;
  reject CL-D3/current as decision IDs; and
- tamper-test swapped order, late score lock, wrong state, artifact mismatch,
  invalid direction, R1/R2 person reuse, retained withdrawn evidence,
  duplicate conditions, and access totals summed from seats.

For Plan 005, the current verifier recognizes phase names but only its inventory,
structural-candidate, and minimal selected paths can succeed. Add
`navigation-task-registry-v1`,
`navigation-open-sort-sessions-v1`, `navigation-open-sort-groups-v1`,
`navigation-open-sort-placements-v1`,
`navigation-open-sort-expectations-v1`, `navigation-open-sort-coding-v1`,
`navigation-open-sort-aggregate-v1`,
`navigation-candidate-formation-v1`,
`navigation-pilot-artifact-manifest-v1`,
`navigation-threshold-pilot-schedule-v1`,
`navigation-threshold-pilot-sessions-v1`,
`navigation-threshold-pilot-trials-v1`,
`navigation-threshold-pilot-aggregate-v1`,
`navigation-threshold-declarations-v1`, `navigation-phase-allocation-v1`,
`navigation-tree-schedule-v1`, `navigation-first-click-schedule-v1`,
`navigation-task-evidence-v2`, `navigation-issues-v1`,
`navigation-formal-aggregate-v1`, and
`navigation-progression-decision-v1`. The validator must:

- implement `sample-plan`, staged `operations`, `open-sort`, `thresholds`, both
  exposure phases, `tree`, `first-click-round-one`, `language-dependency`,
  `first-click`, `decision`, `promotion`, and `final`;
- require explicit flags for the shared attrition ledger and every relevant
  private file; reject missing, unknown, tracked, or wrong-phase inputs;
- recompute open-sort placements, 276 card-pair co-occurrences, coded themes,
  expectation counts, eligible sets, threshold-pilot rational results, all
  eight declarations, formal rows, candidate bytes, hierarchy signatures,
  critical gates, and content-contract hashes;
- require a hash-bound, second-reviewed human candidate-formation record rather
  than pretending an algorithm chose the models; and
- prove every phase and join fails closed with disposable `FIXTURE-` records.
  Fixture IDs are valid only in explicit test mode and operational phases must
  reject them.

### 3.4 Zero-row schema amendment and migration

The normative companion is
`plans/004-005-fieldwork-schema-contract.json`. It contains schemas only—no
people or study results—and is checked by
`plans/validate-004-005-fieldwork-packet.mjs`.

Plan 004 migration registers the maintained 16-column observation matrix,
9-column issue matrix, and the absent `consumer-language-prototypes-v1` schema
witness. Plan 005
registers the maintained four-column `participant-phases.tsv` with
`completion_status = completed | excluded`, 9-column task evidence, and
8-column critical-issue evidence. The contract's `legacyMigrationSources`
records the exact source schema IDs and column counts. Apply these exact steps:

| Maintained source schema | Zero-row target schema |
|---|---|
| `consumer-language-study-v1-observation-matrix` | `consumer-language-task-observations-v2` |
| `consumer-language-study-v1-issues` | `consumer-language-issues-v2` |
| absent `consumer-language-prototypes-v1` formal-manifest schema witness | `consumer-language-exposures-v2` (the recovered draft bytes remain preserved separately) |
| `navigation-participant-phases-v1` | `fieldwork-consent-attrition-v2` |
| `navigation-task-evidence-v1` | `navigation-task-evidence-v2` |
| `navigation-critical-issues-v1` | `navigation-issues-v1` |

No formal `consumer-language-prototypes-v1` file was recovered. Its maintained
exact header is therefore an absent-source schema witness, not a zero-row file
claim. If a post-pilot formal v1 source manifest is later created under the
owner gate, its `manifest_sha256` column remains a legacy omitted-self digest:
preserve that cell byte-for-byte, but do not treat it as a v2 trust decision or
insert it into the v2 hash DAG. In every v2
`prototype_manifest_sha256` field, use the SHA-256 of the entire strictly
validated post-pilot v1 file bytes, including its legacy digest column and
final LF. The v2 input manifest independently records that same external file
SHA. All files created by this packet follow the no-self-hash rule; a future
legacy in-row value is never an exception for a new file.

1. For each maintained v1 schema, confirm whether a private file exists. No
   such file is currently available to this packet. For an absent source,
   create no pretend legacy file: hash the exact maintained header plus one LF
   as its schema witness. If a private file is later supplied, hash it read-only,
   verify its exact header, and count its data rows.
2. Emit `source_presence = absent-schema-witness`, `source_path_code = n/a`,
   `source_evidence_sha256 = SHA-256(exact header + LF)`, row count `0`,
   `zero_row_assertion = true`, and
   `migration_kind = absent-source-schema-witness` for the currently absent
   sources. A verified present empty file instead uses `present-private-file`
   and `zero-row-file-replacement` with its actual path code/file SHA.
3. Require zero Plan 004 observation rows, zero Plan 005 participant rows, and
   the preserved tracked zero aggregates. That is the only observed state and
   creates no participant event. Create empty exact-header v2 files. Never infer or backfill consent,
   direction, order, state, access, withdrawal, or exclusion.
4. If any v1 row or unexpected file is discovered, stop and emit
   `present-private-file` plus `quarantined-nonzero-source`; do not transform it.
   A Plan 004 row cannot be migrated without
   missing semantics. A Plan 005 `completed` row without independent retained
   consent becomes `legacy-missing-consent`; `excluded` becomes
   `legacy-unresolved`; both remain ineligible until human source recovery.
5. Emit one exact `fieldwork-schema-migration-v1` row per maintained source:
   source schema/presence/path code, exact 40-hex source Git commit,
   evidence/header hashes and row count; the table's target
   schema/path/header hash; the presence-matched migration kind above;
   `zero_row_assertion = true`; old/new canonical aggregate hashes; actor/time;
   and the owner comment URL/body hashes. The singular target is always the TSV
   schema in the table, never an aggregate. The separate old/new aggregate
   fields bind Plan 004's `consumer-language-aggregate-v2` or Plan 005's
   `navigation-open-sort-aggregate-v1`,
   `navigation-threshold-pilot-aggregate-v1`, and
   `navigation-formal-aggregate-v1` zero closures as their phases activate.
6. Obtain owner-authored, unedited schema-amendment comments on #37 and #38 and
   bind their URLs/body hashes before formal evidence. Executor prose is not
   approval.
7. After activation, the old Plan 005 `participant-phases.tsv` may exist only
   as a generated compatibility view: eligible included rows project to
   `completed`; withdrawn, excluded, stopped, deleted, or invalid rows project
   to `excluded`. The shared event ledger remains authoritative.

## 4. Operator resource manifest

The operator supplies this completed manifest outside Git. The repository may
retain only non-sensitive decisions and approval coordinates. Never commit
credentials, contact lists, names, payment details, consent forms, or private
store paths that reveal a person's identity.

```text
packet_version: FW-004-005-v2
step_1_landed_sha: 9fc7dcacfc961752e5d9a2cedbc426deead54a05
coordinator_release_message: <durable message locator>
recruitment_operator: <named accountable person or organization>
recruitment_channel: <specific existing channel>
channel_authority: <who can use it and proof of permission>
participant_contact_owner: <named person>
contact_window: <dates/times/time zone>
real_target_user_access_basis: <how the channel reaches the screened segments>
session_mode: <in-person | remote | mixed>

compensation_terms_schema: fieldwork-compensation-terms-v1
compensation_terms_file_sha256: <validated external private file hash>
compensation_terms_by_phase: <exact eight-row matrix: p004-pilot; p004-r1; p004-r2; p005-open-sort; p005-threshold-pilot; p005-tree-test; p005-first-click-r1; p005-first-click-r2>
per_phase_required_fields: <paid|unpaid; exact amount/currency or none; funder/proof or n/a; delivery rail/deadline or n/a; withdrawal; no-show; cancellation; participant-terms hash>

contact_registry_system: <named approved system and non-secret locator>
contact_registry_custodian: <named person>
contact_registry_access: <named identities/roles; recruitment/payment only>
contact_registry_controls: <MFA, encryption, audit/access-review policy>
contact_registry_data_classes: <contact; contact consent; scheduling; payment if used; person-key/study-ID link; privacy-minimal cohort-reuse/suppression token; withdrawal/deletion requests>
contact_registry_retention_trigger_and_deadline: <exact rule>
contact_registry_deletion_and_backup_process: <exact primary/backup confirmation process>
contact_registry_withdrawal_handling: <who receives, authenticates, relays, and closes a request>
cohort_reuse_token_method: <keyed HMAC over normalized operator-held contact coordinate; key custodian/rotation; no raw contact in research store>
cohort_reuse_token_custodian_and_access: <named minimum-necessary contact-registry roles>
cohort_reuse_token_retention_and_deletion: <through final cross-plan recruitment/reuse check, then exact deletion and backup purge>

research_evidence_system: <different named approved system and non-secret locator>
research_evidence_custodian: <named person; may not resolve identities>
research_evidence_access: <named moderator/researcher/reviewer identities or roles>
research_evidence_controls: <MFA, encryption, audit/access-review policy>
research_evidence_data_classes: <recruitment keys; de-identified screener/quota rows; opaque person/study IDs; consent/attrition events; raw matrices/notes; artifact joins; no contact/payment data or reuse token>
plan_004_retention_trigger_and_deadline: <later of latest applicable disclosed cutoff and the Plan 004 ordinary earlier-of deadline; cutoff no later than Plan 004 180-day ceiling>
plan_005_retention_trigger_and_deadline: <independent later of latest applicable disclosed cutoff and the Plan 005 ordinary earlier-of deadline; cutoff no later than Plan 005 180-day ceiling>
research_evidence_deletion_and_backup_process: <exact primary/backup confirmation process>
research_evidence_withdrawal_handling: <scope resolution, raw deletion, aggregate invalidation/reclosure>

moderator: <named human>
incident_owner: <named human and private response path>
second_human_reviewer: <named authorized human>
research_devices: <provided device inventory or participant-device policy>
required_browsers_and_versions: <exact matrix>
required_at_facilities: <keyboard, screen reader, enlargement/reflow, motor/cognitive support path>
prototype_exposure_method: <exact device, local method, or approved private URL>
prototype_exposure_boundary: <who can access it and for how long>
recording_used: false
recording_tool_storage_retention_deletion: n/a
observer_roles: <moderator; optional disclosed note-taker/reviewer>
withdrawal_window: <exact participant-facing rule and computed cutoff>

plan_004_pr_37_gate_comments: <owner-authored structured comment URLs/body hashes>
plan_005_pr_38_gate_comments: <owner-authored structured comment URLs/body hashes>
```

The two private systems must be access-separated in fact. One shared account,
folder, public link, or nominally different path does not satisfy the boundary.
Only the contact registry may resolve `program_person_key` or `study_id` to a
person. Research roles receive opaque keys; contact/payment roles do not receive
raw observations. A withdrawal/deletion request enters through the contact
registry and is relayed by opaque key to the evidence custodian.

If `recording_used` changes to `true`, the operator must also supply the exact
tool, recording types, viewers, storage location, separate opt-in language,
retention deadline, and deletion confirmation process. Declining recording may
not make an otherwise eligible person ineligible; structured notes are the
fallback.

The approval sequence remains machine-bound to the existing draft PRs. Before
recruitment, #37/#38 receive their amended schema/operations/sample comments.
Before each exploratory exposure, the phase-specific pilot/open-sort artifact
and protocol coordinates are recorded privately; they are not formal exposure
approvals. Before each formal exposure, #37 receives the exact source-manifest
plus isolated-exposure-set approval, and #38 receives the exact candidate,
schedule, access-method, and exposure approval. Progression/decision and
canonical-promotion comments occur only at their later gates. Use exact owner-
authored comment grammars; executor prose or the manifest itself is not an
approval.

## 5. Target segments and sample plan

### Counted primary participants

- age 18 or older;
- active, recent, or likely entry-level civil-service custodial/janitorial
  candidates for Plan 004;
- active or likely civil-service applicants or adjacent public-service job
  seekers for Plan 005, with primary candidates remaining the majority in every
  counted phase;
- able to use the canonical English product, including limited-English readers;
- a real prospective learner, not a generated persona, role-played friend,
  agent, project contributor, or professional proxy; and
- willing to avoid all remembered or secure exam content.

Librarians and workforce-development staff may be interviewed separately as
support users. They do not satisfy a learner minimum and their evidence is
reported separately.

Recruit for phone-primary use, varied reading and digital confidence, and
access strategies without asking for a diagnosis. Plan 005 must include at
least four completed participants across its program who regularly use one or
more of keyboard-only navigation, screen reader, magnification/text
enlargement, motor adaptation, or cognitive/literacy support. Record the
strategy category, not medical information.

### Phase seats

| Phase | Effective target | Scheduled length | Reuse rule |
|---|---:|---:|---|
| Plan 004 guide/prototype pilot | 1 | 75 min | Uncounted; exposed cohort is not reused |
| Plan 004 R1 | exactly 6 or 8 | 60 min | Formal candidate/control evidence; exposed cohort is not reused |
| Plan 004 R2 | exactly 6 or 8 new | 60 min | Retests all R1 criticals; exposed cohort is not reused |
| Plan 005 open sort | 6–8 | 45 min | No candidate IA or Plan 004 label exposure; exposed cohort is not reused |
| Plan 005 threshold pilot | 2–3 | 60 min planned; rerun a new version if the pilot shows this is insufficient | Eligible only for pilot thresholds; exposed cohort is not reused |
| Plan 005 tree test | 15–20 | 45 min | Fresh cohort; one candidate per person |
| Plan 005 first-click R1 | 5–8 | 45 min | Fresh cohort; one candidate per person |
| Plan 005 first-click R2 | 5–8 new | 45 min; 60 with an approved access accommodation | Fresh cohort; language-reconciled selected structure only |

Plan 004 uses six or eight, both inside the reviewed 5–8 range, because the
locked allocation requires direction/order balance; seven is not a valid
formal size. Plan 005 retains its recommended ranges and non-waivable floors.
Any feasibility deviation must be approved before contact and may not be
lowered after results are visible.

### Cohort-reuse matrix

`NO` means the same `program_person_key` may not appear in both exposed phases.
Screening without any study material is not exposure and does not bar later
assignment. Every exposed cohort is otherwise disjoint; this prevents open-sort
authors, pilot learners, tree learners, or earlier first-click participants
from carrying learned hierarchy or language into a later scored phase.

| Earlier exposed phase | P004 pilot | P004 R1 | P004 R2 | P005 open sort | P005 threshold | P005 tree | P005 F1 | P005 F2 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| P004 pilot | — | NO | NO | NO | NO | NO | NO | NO |
| P004 R1 | NO | — | NO | NO | NO | NO | NO | NO |
| P004 R2 | NO | NO | — | NO | NO | NO | NO | NO |
| P005 open sort | NO | NO | NO | — | NO | NO | NO | NO |
| P005 threshold | NO | NO | NO | NO | — | NO | NO | NO |
| P005 tree | NO | NO | NO | NO | NO | — | NO | NO |
| P005 F1 | NO | NO | NO | NO | NO | NO | — | NO |
| P005 F2 | NO | NO | NO | NO | NO | NO | NO | — |

Count seats and unique people separately. Plan 005's access minimum is
`COUNT(DISTINCT program_person_key)` across eligible, completed included rows
with an `access-strategy-used` event. It is not a sum of phase seats or of
overlapping strategy categories. The same rule applies to program-wide Plan 004
access summaries.

Replacement participants receive a new study ID and person keys never change or
recycle. Never recycle an excluded or withdrawn ID. Attrition is derived from
the shared event ledger and reported as invited, screened, scheduled, started,
completed, excluded, and withdrawn counts without names.

The contact registry also computes a privacy-minimal keyed-HMAC reuse/suppression
token from its normalized contact coordinate. It retains only the token → opaque
`program_person_key`/exposed-phase flags through the final cross-plan
recruitment window, even when an identity/study link is deleted earlier. The
method, purpose, custodian, access, retention, and deletion date are disclosed
before contact. The HMAC key and token never enter the research store or Git.
After the last reuse check, the custodian deletes the token map and backups under
the manifest rule. A program withdrawal adds suppression and can never be
overridden by a later narrow consent; a phase-scoped request still preserves
only the minimum token needed to enforce the already-exposed cohort ban.

## 6. Screener — exact script and routing

Before the first answer, the contact registry creates a random
`RK-[0-9a-f]{32}` recruitment key and sends only that key plus intended
plan/phase to the approved research-store screener writer. The exact
`fieldwork-screener-v1` row contains de-identified routing/quota answers and no
name, contact coordinate, payment value, or free text. The contact registry
keeps identity/contact and its reuse token; the research store keeps the
screener row. A `program_person_key` may be `n/a` while screening and is created
after contact consent/eligibility, before any exposure. Screening and scheduling
events use `study_id = n/a`.

An exploratory study ID is created only after that phase's section 2.2 gate; a
formal ID only after its exact section 2.3 row. Activation appends
`study-id-activated` to the shared ledger with the existing recruitment/person
keys and the new phase ID. No formal ID, link, allocation assignment, or exposed
artifact may be prepared merely because screening passed.

Recruitment copy must say the project is independent/unofficial, is testing a
study site rather than the person, never wants remembered exam material, and
states exact duration plus the approved paid or unpaid terms. The recruiter
asks:

1. **Are you 18 or older?** `No` ends screening.
2. **Which best describes you?** `preparing/recently prepared for an
   entry-level custodian or janitor civil-service exam`; `considering that kind
   of job`; `applying for another entry-level public-service job`; `helping job
   seekers`; `none`. The first two are Plan 004 primary; the first three may be
   Plan 005 learners; helpers are secondary only.
3. **Is your interest entry-level, higher-level/supervisory, or not sure?**
   Higher-level-only applicants are excluded from counted entry-level evidence.
4. **What device do you normally use first for websites?** `phone`; `tablet`;
   `computer`; `other`. This is a quota field, not an exclusion.
5. **How comfortable are you finding information or changing settings on a
   website?** `not very`; `somewhat`; `very`. Recruit a mix.
6. **When using a free study website, do you normally expect to use it without
   making an account?** `yes`; `no`; `not sure`. This is a segment field, not an
   exclusion.
7. **Do you regularly use any of these when browsing?** `keyboard without a
   mouse`; `screen reader`; `zoom/larger text/magnifier`; `voice or motor
   adaptation`; `help reading/understanding`; `none`; `prefer not to say`.
   Record only the chosen category.
8. **Can you read and respond to an English-language prototype, even if English
   is not your first language?** `No` is excluded because reviewed translation
   support is not available in this protocol.
9. **Have you already joined any phase of this project?** Record study phase/ID
   if known without asking for exam details; apply the reuse rules above.
10. **Do you work on this repository, design this site, or conduct this study?**
   `Yes` excludes the person from target-user counts.
11. **During the session, can you avoid sharing anything remembered from a real
    exam—questions, choices, drawings, answers, review notes, or admission
    documents?** `No` ends screening without collecting the material.
12. **Are you available for the stated session length and mode?** Use the
    operator's real schedule.
13. **May the research operator contact you only about scheduling, payment if
    this phase is paid, and any withdrawal or deletion request you make?** `No`
    ends recruitment. This contact consent does not equal research consent.
    Contact details go into the contact/payment registry, never the research
    matrix or Git.

Do not ask for an employer, applicant/admission number, exact location,
disability diagnosis, exam recollection, or proof of candidacy. A public exam
category may be used for quotaing; exact personal application documents may not.

## 7. Consent, compensation, recording, and withdrawal

### Consent script

The moderator reads this after replacing every bracketed operator field in the
private run sheet:

> We are testing an independent practice website for New York entry-level
> custodian and janitor exams. The site is not run by a government agency. We
> are testing the website, not you, and there are no performance rewards or
> penalties.
>
> This session takes about [MINUTES] minutes. I will ask you to group study
> tasks, find information, or explain what words on a prototype mean. You may
> skip any question, take a break, or stop at any time without giving a reason.
> [PAID VERSION: This session pays [AMOUNT AND METHOD]. If the session starts
> and you stop, you still receive the promised amount.] [UNPAID VERSION: This
> session is unpaid. You may stop or skip without penalty; no payment is
> offered.]
>
> Please do not share anything remembered from a real exam: no questions,
> answer choices, drawings, answers, review-session notes, admission documents,
> or photographs. If that starts to happen, I will interrupt and move on. We
> will not write down or repeat the material.
>
> We will take structured notes about what you do and say about the prototype.
> [NO RECORDING WILL BE MADE / EXACT SEPARATE RECORDING STATEMENT]. Your contact
> and payment information is kept separately from study notes. Raw notes are in
> [PRIVATE SYSTEM NAME], available only to [ROLES], and follow the deletion
> schedule I will give you. GitHub receives only de-identified totals and
> themes.
>
> You can ask us to delete your identifiable notes until [WITHDRAWAL CUTOFF] by
> contacting [OPERATOR CONTACT]. After results have been de-identified and
> combined, we may no longer be able to separate your contribution. The main
> risks are ordinary frustration with a prototype and accidental disclosure of
> personal or exam information; we will stop and remove that material if it
> occurs. There is no guaranteed personal benefit.

The moderator then asks, separately:

1. Paid: “Can you stop or skip a task and still receive the promised
   compensation?” Unpaid: “Can you stop or skip a task without penalty, and is
   it clear that this session offers no payment?”
2. “Should you tell us a question or answer you remember from a real exam?”
3. “Do you agree to take part and to the note-taking just described?”
4. If recording is approved: “Do you separately agree to [audio/screen] recording?”

Research consent is complete only after a correct `yes` to the applicable terms
question, `no` to sharing remembered exam material, and `yes` to
research/note-taking (items 1–3). Correct misunderstandings once and ask again.
Otherwise stop without issuing a scored participant row. When recording is
approved and offered, item 4 controls recording only: `yes` permits the named
recording, while `no` records `recording-consent-declined` and the session
continues on the required no-recording path without exclusion.

### Compensation policy

- When used, compensation is for time, not performance, agreement, or
  completion. An unpaid phase must be labeled unpaid in every contact and
  consent surface.
- Before recruitment, the operator records either exact paid terms and verifies
  the funds/payment rail or records exact unpaid terms with amount `none`.
- For a paid phase, pay the full promised session amount when a participant
  withdraws after the session starts or the researcher stops for security,
  privacy, accessibility, distress, or technical reasons. For an unpaid phase,
  do not imply that compensation exists; stopping remains penalty-free.
- Recruitment copy states payment timing. The default operational target is
  delivery within two business days; a different timing must be disclosed and
  approved before contact.
- No-show and advance-cancellation terms must be stated before scheduling.
- A no-pay design is valid only as an explicit operator decision stated in the
  recruitment and consent materials; silence is not a no-pay decision.

### Recording policy

Default: no audio, video, screen, or keystroke recording. One moderator and,
when disclosed, one note-taker use structured notes. If recording is later
approved, consent to research and consent to recording remain separate;
declining recording cannot exclude a participant, and the no-recording session
path remains available.

### Withdrawal and deletion

- Stop immediately; do not ask why.
- Append `participant-withdrew` to `fieldwork-consent-attrition-v2` with exact
  `phase | plan | program` target scope; never overwrite a completed/excluded
  cell or revive a broader terminal event with narrower later consent.
  Every observation in scope is ineligible at the aggregation cutoff, including
  observations that occurred before the request.
- Pay according to the full-session withdrawal rule.
- Before the stated cutoff, delete raw notes and any recording, invalidate and
  recompute unpublished phase manifests/aggregates, and append only the
  approved opaque deletion tombstone. Immediately restrict/minimize the
  contact-registry identity/person/study resolving link to what is necessary to
  authenticate and close the request and settle any payment; delete that link
  at the exact registry deadline in section 8. The separately disclosed
  reuse/suppression token follows its independent program-end deadline.
- Until each person's disclosed withdrawal cutoff, keep the private inclusion
  map reversible and do not publish or mark an aggregate irreversibly
  de-identified. `aggregate-no-longer-separable` is prohibited before that
  cutoff. A phase aggregate cannot be accepted or published until strictly
  after every included person's applicable cutoff. After a truthfully
  disclosed cutoff and irreversible publication,
  explain the stated limit; do not pretend a row can be recovered.

`contact-consent-revoked` stops future contact/scheduling and payment messages
not needed to close an existing obligation; it does not erase valid structured
evidence. `recording-consent-revoked` deletes and invalidates the recording only;
it does not invalidate separately consented structured notes. Only applicable
research-consent revocation, participant withdrawal, deletion within the
cutoff, researcher stop, or evidence exclusion removes structured evidence from
an aggregate. A skipped task is neither consent revocation nor withdrawal.

The shared append-only ledger is the consent/attrition authority for both
plans. An eligible denominator requires research consent before exposure,
terminal session completion, no applicable later research-consent revocation/withdrawal/
deletion/stop/exclusion at the named closure cutoff, and valid phase-specific
artifact/protocol joins. Plan 005's legacy `completed | excluded` value is only
a derived compatibility projection: withdrawal projects to `excluded`, but its
event type, scope, and reason remain unambiguous in the shared ledger. A skipped
task is not withdrawal; apply the task rule without changing phase consent.

## 8. Private data, retention, and Git boundary

The operator provisions two access-separated private systems:

1. **Contact/consent/payment registry** — names and contact/payment status,
   scheduling, contact consent, opaque person/study links, privacy-minimal
   reuse/suppression token, and withdrawal/deletion requests; accessible only
   to the recruitment/payment operator and its named custodian.
2. **Research store** — recruitment keys, de-identified screener/quota rows,
   study IDs, structured observations, code-keyed notes, consent/attrition
   events without identity, prototype/evidence coordinates, and incident codes;
   accessible only to the named moderator/researcher, evidence custodian, and
   minimum-necessary second reviewer. It contains no contact value, payment
   coordinate, or cohort-reuse token.

The resolving ID link stays only in the contact registry. The research store
may use the same opaque `program_person_key` for reuse/unique-person checks but
cannot resolve it to identity. The systems need separately named custodians and
access lists; one account or shared folder cannot satisfy both. Both require
authenticated accounts, least privilege, MFA where available, encryption in
transit and at rest, audit/access review, and deletion from recoverable backups
under the named process. Shared public links, personal unencrypted folders,
issue attachments, chat uploads, and `/tmp` are not private systems.

### Shared consent/attrition schema

The research store's authority is append-only
`participant-consent-attrition.v2.tsv`, schema
`fieldwork-consent-attrition-v2`, with this exact header:

```text
schema_version\tevent_id\trecruitment_key\tprogram_person_key\tstudy_id\tevent_at_utc\tevent_type\ttarget_scope\ttarget_plan_id\ttarget_phase_id\tconsent_document_sha256\twithdrawal_cutoff_at_utc\tcompensation_terms_code\tcompensation_terms_sha256\treason_code\taccess_strategy_code\tactor_code\tprior_event_id
```

`recruitment_key` is random `RK-[0-9a-f]{32}` and exists before screening.
`program_person_key` is random opaque `PPK-[0-9a-f]{32}`, assigned only after
eligibility/contact consent and stable across both plans; only the contact
registry can resolve it. Before activation `study_id` is `n/a`. Phase study IDs
are `PILOT-P[0-9]{2}`, `R1-P[0-9]{2}`,
`R2-P[0-9]{2}`, `OS-P[0-9]{2}`, `TP-P[0-9]{2}`,
`TT-P[0-9]{2}`, `F1-P[0-9]{2}`, or `F2-P[0-9]{2}`. Event IDs are unique opaque
`EVT-[0-9a-f]{32}` values; times are UTC RFC 3339. `prior_event_id` forms one
acyclic chronological chain per recruitment key (`n/a` only on its first event),
so program/plan events apply to every same-`program_person_key` study ID.

Exact event types are `screened-eligible`, `screened-ineligible`,
`contact-consent-affirmed`, `contact-consent-declined`,
`contact-consent-revoked`, `research-consent-affirmed`,
`research-consent-declined`, `research-consent-revoked`,
`study-id-activated`,
`recording-not-used`, `recording-consent-affirmed`,
`recording-consent-declined`, `recording-consent-revoked`, `scheduled`,
`session-started`, `session-completed`, `participant-withdrew`,
`researcher-stopped`, `excluded`, `deletion-requested`,
`raw-deletion-confirmed`, `aggregate-no-longer-separable`, and
`access-strategy-used`.

`target_scope` is `phase | plan | program | contact | recording | n/a`.
`phase` requires both target plan and phase; `plan` requires a plan and
`target_phase_id = n/a`; `program`, `contact`, `recording`, and `n/a` require
both targets `n/a`. Phase IDs are the eight exposed phase names in the cohort
matrix. `compensation_terms_code` is `paid | unpaid | n/a` and an activated
phase joins the exact `fieldwork-compensation-terms-v1` row/file hash. Each
`research-consent-affirmed` event requires the exact consent document SHA-256
and the participant-specific UTC `withdrawal_cutoff_at_utc` disclosed by that
document; other events use `n/a` for the cutoff. The cutoff must be no later
than the applicable plan's 180-days-after-last-session ceiling. Consent events
retain the exact consent document SHA-256.
Reason is `none | participant-request |
consent-incomplete | security-interruption | artifact-mismatch |
technical-failure | facilitator-contamination | duplicate | ineligible |
protocol-deviation | legacy-missing-consent | legacy-unresolved`.
Access strategy is `n/a | none | prefer-not-to-say | keyboard-only |
screen-reader | magnification-reflow | motor-adaptation |
cognitive-literacy-support`; emit one event per strategy actually used.
Any multi-value screener/participant/task column uses the same closed set,
lexically sorted, unique, and joined by one `+`; `n/a` is used only where the
schema permits it.

For one event type, later time/event ID wins only within equal scope. Applicable
scope precedence is `program > plan > phase`; a broader research withdrawal,
revocation, deletion, or stop is terminal and a later narrower consent cannot
revive it. An event keyed to one study propagates through
`program_person_key` to every study in its target scope. Contact revocation has
contact-only effect. Recording revocation changes recording to `withdrawn` and
requires recording deletion but leaves structured-note consent unchanged.
Research consent maps `affirmed → consented`, `declined → declined`, and
applicable `revoked`/`participant-withdrew → withdrawn`; phase status uses
`scheduled`, `session-started`, `session-completed`, `excluded`, or
`participant-withdrew`; deletion is `not-requested`, `pending`, `completed`, or
`irreversibly-deidentified`. Applicable research exclusion/stop/withdrawal or a
timely deletion forces aggregate ineligibility even if completion is earlier.
Task-only technical/protocol exclusions remove only named task rows; phase-level
events remove the entire phase. The compatibility projection is therefore
deterministic and cannot be overridden by a phase file's entered status.

The exact `research-consent-affirmed` `event_id` is
`research_consent_event_id` in every participant/session record; it must match
the same recruitment/person/study keys and predate exposure. The contact
registry separately retains contact-consent proof, its short-lived resolving
map, and the longer privacy-minimal reuse/suppression token described above.
Mirrored consent/status fields must byte-match the effective projection;
disagreement fails. The ledger stores no name, contact value, payment
coordinate, diagnosis, free-text note, or exam content.

At each named closure cutoff, the verifier derives—not hand-enters—one
`fieldwork-attrition-projection-v1` row per activated study. The exact private
file binds projection version, source-ledger external SHA, cutoff, recruitment/
person/study/plan/phase joins, effective research/recording/contact/phase/
exclusion/deletion states, each decisive event ID, applicability at program/
plan/phase scope, the consent-bound withdrawal cutoff, deletion-request
timeliness (`no-request | timely | late-after-cutoff`), and eligibility/reason.
Every non-`n/a` event ID must resolve
to the same person and applicable scope. Its canonical external file SHA is the
only `attritionProjectionSha256` used by input eligibility, aggregates, and the
final phase manifest.

When more than one ineligibility condition applies, choose exactly the first
reason in this precedence list: `deleted`, `deletion-pending`,
`participant-withdrew`, `research-consent-revoked`, `researcher-stopped`,
`excluded`, `artifact-or-protocol-invalid`, `phase-incomplete`,
`research-consent-declined`, `missing-research-consent`, then `eligible`.
Exclusion aggregates group those derived reason codes; an operator never picks
the most convenient reason.

Retention schedule (computed independently for each plan):

- the identity/contact → person/study resolving link is deleted seven days after
  the later of paid compensation resolution (or session closure when unpaid)
  and that person's disclosed withdrawal cutoff; the separately disclosed
  keyed-HMAC reuse/suppression token → opaque person/exposure-flags map remains
  through the final cross-plan recruitment/reuse check and is then deleted from
  primary storage and recoverable backups on its independently recorded date;
- Before contact, every disclosed withdrawal cutoff for a plan must be on or
  before that plan's 180-days-after-last-session ceiling. If it is not, the
  participant terms must be corrected before contact; after collection begins,
  an incompatible cutoff stops collection and cannot be shortened
  retroactively.
- For Plan 004, first compute the ordinary deadline as the earlier of 30 days
  after the accepted Plan 004 findings merge and 180 days after the last Plan
  004 session. Delete Plan 004 raw notes, matrices, label/control artifacts,
  and any approved recording on the **later** of that ordinary deadline and the
  latest applicable disclosed Plan 004 withdrawal cutoff. A stopped Plan 004
  uses the same plan-specific rule and ceiling.
- For Plan 005, independently compute the ordinary deadline as the earlier of
  30 days after the accepted Plan 005 findings merge and 180 days after the
  last Plan 005 session. Delete Plan 005 raw notes, matrices, sort labels,
  candidate/pilot material, and any approved recording on the **later** of that
  ordinary deadline and the latest applicable disclosed Plan 005 withdrawal
  cutoff. Plan 004 activity never extends this clock, and vice versa.
- Raw evidence, the private inclusion map, and person/study linkage needed to
  honor a request may not be deleted or irreversibly aggregated before the
  applicable participant cutoff. A retention date that would arrive first is
  superseded by that cutoff under the formulas above.
- a timely withdrawal/deletion event can require earlier scoped deletion and
  reclosure of an unpublished phase manifest; and
- each custodian records deletion and backup-purge confirmation outside Git.
  Git retains
  only de-identified aggregate counts, findings, artifact hashes, limitations,
  and decisions.

Consent/financial records that an operator must retain under a separate legal
policy are outside this research corpus and must be disclosed by that operator;
this packet does not invent a legal retention duty.

## 9. Prototype exposure method

Before each exploratory phase or formal round, the operator records one exact
method:

- a facilitator-controlled research device serving the immutable snapshot only
  on loopback for an in-person/lab session; or
- an operator-controlled private static host whose URL, access boundary, and
  expiry are recorded without credentials.

A phone-first or access-strategy participant must interact on a device and
browser that supports the method being studied; watching a facilitator click
is not first-click or assistive-technology evidence. No public search indexing,
analytics, third-party behavioral tracking, production modules, live learner
data, or answer-bearing precommit material is allowed.

Every exploratory exposure uses safety-reviewed bytes whose exact working hash
is recorded before the session; those bytes may be revised afterward and are
never called R1/R2. Every formal exposure uses immutable bytes named by the
current round's structured approval. The facilitator checks the appropriate
pilot inventory or formal manifest before the session and records the exact
artifact, state, candidate/direction, and order code as exposure begins. An
unhashed exploratory artifact, or a mutable/mixed/unapproved formal artifact or
URL, invalidates the session.

## 10. Common session opening, roles, and rescue rules

Roles: one moderator; optionally one disclosed note-taker. Observers stay
silent, do not message the moderator during tasks, and never contact a
participant directly. The moderator:

1. verifies the readiness ledger, participant ID, consent, artifact hash, and
   assigned counterbalance row;
2. says, “Please think aloud. I may stay quiet because I want to see what the
   page communicates on its own”;
3. never asks “Do you understand?”, teaches a label, names the expected route,
   or praises a choice;
4. uses only neutral probes: “What are you looking for?”, “What would you do
   next?”, “What makes you say that?”, and “What do you expect to happen?”;
5. after 30 seconds of no first action, repeats the task once; after the locked
   phase timeout or a request for help, records failure before offering a rescue;
6. takes a break at the participant's request and offers one at 30 minutes; and
7. closes by restating deletion/contact information and payment timing without
   disclosing a correct research answer.

Facilitator rescue never counts as success. A technical restart may continue
only if the same immutable artifact, state, order, and task remain intact; else
the session is technically invalid and may be rescheduled under a new ID.

## 11. Plan 004 sessions

### Pilot (one real person; not evidence)

Run the complete guide and repaired prototypes. Test timing, isolation of the
assigned direction/state, task ambiguity, control behavior, screen-reader or
zoom behavior where applicable, and note codes. Do not enter the pilot into R1
or aggregate findings. Revise editable files and script; only then freeze R1,
make it non-writable, compute its manifest, obtain the R1 exposure approval,
and rerun the verifier.

### R1 and R2 task order

Use the same natural-journey order in both rounds. R2 uses new participants and
must retest every R1 critical issue.

1. `proposition-recall` — show assigned Home for ten seconds, hide it, and ask:
   “What is this site, who is it for, is it run by government, and what would
   you do first?”
2. `profile-fit` — use this synthetic scenario: “A school district in upstate
   New York sent you a public exam notice for a custodian job, but this card
   does not include an exam number.” Ask: “Does this site cover that exam, and
   how can you tell?”
3. `practice-commitment` — ask the participant to start practice and, before
   acting, explain what happens to a chosen answer.
4. `feedback-evidence` — from post-answer feedback ask what was right, why, and
   where the support can be checked.
5. `hazard-feedback` — ask the participant to distinguish one missed hazard
   from one safe detail marked by mistake.
6. `review-meaning` — ask what the list means and what acknowledging an item
   will do.
7. `offline-failure` — show a failed update and ask what happened and what still
   works.
8. `import-reset` — show import and reset previews and ask exactly what changes
   and what stays.
9. `advanced-evidence` — ask where to find the exact source and version without
   putting those details in the default path.

Use an independently reviewed synthetic scenario; never use a participant's
announcement, application, or remembered material.

### Locked Plan 004 task/state/variant joins

The formal exposure manifest and every task row use this exact mapping. A
different task/prototype/state pair is an artifact mismatch, not a new finding.
`copy_variant_id` resolves the assigned CL-D1/CL-D2 or current-control bytes in
the isolated exposure set.

| Task ID | Prototype ID | State ID | Condition/variant family |
|---|---|---|---|
| `proposition-recall` | `home` | `home-first-use-ready` | `candidate/default` |
| `profile-fit` | `profile` | `profile-scope-undetermined` | `candidate/guardrail` |
| `practice-commitment` | `practice-start` | `answer-selected-uncommitted` | `candidate/default` |
| `feedback-evidence` | `question-feedback` | `question-postcommit-feedback` | `candidate-or-current/rationale-source` |
| `hazard-feedback` | `hazard-feedback` | `hazard-miss-false-positive` | `candidate/feedback` |
| `review-meaning` | `review` | `review-ready` | `candidate/default` |
| `offline-failure` | `offline-data` | `update-failed-prior-usable` | `candidate-or-current/recovery` |
| `import-reset` | `offline-data` | `import-preview-reset-confirm` | `candidate-or-current/destructive-guardrail` |
| `advanced-evidence` | `trust-recovery` | `source-detail-closed` | `candidate/advanced` |

### Plan 004 direction allocation

Set the formal effective sample to exactly six or eight before contact. The
three comparison tasks, in bit order, are `feedback-evidence`,
`offline-failure`, and `import-reset`; bit `0` means current first and bit `1`
means candidate first. With six people use exactly `001`, `010`, `011`, `100`,
`101`, and `110`. With eight use `000` through `111`. In R1 even-parity patterns
are CL-D1 and odd-parity patterns are CL-D2; reverse those directions in R2.
This balances each task's order overall and keeps every direction/order cell
within one person.

After the effective `n` and phase version are approved, but before the first
formal study ID, generate one private 32-byte operating-system CSPRNG seed and
freeze the complete `consumer-language-allocation-v1` file. For each balance
pattern create one `primary`, `reserve-1`, and `reserve-2` occurrence. A reserve
names its primary in `replacement_of_allocation_slot` and repeats that primary's
direction and three order values. Hash the exact UTF-8/LF string
`consumer-language-allocation-v1\nphase=<p004-r1|p004-r2>\nschedule=<version>\nseed=<64hex>\nslot=<bits>\nrole=<primary|reserve-1|reserve-2>\n`,
sort raw hashes within each role, fail on collision, and serialize the whole
schedule canonically. Its external file SHA-256 is recorded in every participant
row; the schedule contains no self hash.

After individual consent, assign the next unused primary occurrence. If that
person withdraws or has an invalid candidate condition, never reuse their ID or
occurrence: assign a new person/new ID the next unused reserve for that same
primary balance cell. Outcome data never changes reserve order. If both reserves
for a cell are exhausted, stop the round; a new approved version and fresh
schedule/cohort are required. The included candidate sample is exactly six or
eight completed balance cells, with each primary cell represented once.

Every participant completes nine candidate conditions and three independently
scored current controls. For a comparison pair, score and timestamp the first
condition before exposing the second; score the second independently. Only
after both scores lock may the moderator ask a comparative probe. Candidate
and current outcomes are never pooled or overwritten. Candidate rows control
direction eligibility; controls are reported separately by task and order. A
wrong order, comparative teaching before score lock, or an exposure whose
first score locks after the second begins is contamination: exclude that
condition as `protocol-deviation`. An invalid candidate condition makes the
formal participant candidate-ineligible and triggers the same-cell reserve
rule. A control-local failure after the candidate score has already locked is
excluded from control and paired denominators without erasing that candidate
row. If current was first, or wrong order, leading, rescue, wrong bytes, or any
other control event occurred before the candidate score locked, the candidate
is contaminated too and the same-cell reserve rule applies. All three controls
must still have been attempted and every exclusion reason retained.

The exact allocation, participant, exposure, task, and issue headers are in
`plans/004-005-fieldwork-schema-contract.json` under
`consumer-language-allocation-v1`, `consumer-language-participants-v2`,
`consumer-language-exposures-v2`,
`consumer-language-task-observations-v2`, and
`consumer-language-issues-v2`. Formal exposure approval binds both the retained
`consumer-language-prototypes-v1` source manifest and the isolated
`consumer-language-exposures-v2` artifact-set version/hash. The latter proves
which one-direction, one-state, one-condition bytes were actually shown.

`consumer-language-participants-v2` is one row per phase/study ID. It binds the
opaque person/session and exact same-person/same-study
`research_consent_event_id`, consent version/time,
recording decision, direction, allocation schedule/slot/order, access strategy,
phase status, exclusion/reason, withdrawal scope, and deletion status. Formal
direction is exactly CL-D1 or CL-D2. The pilot instead uses
`consumer-language-pilot-v1`; its session is unscored, while each row records
the exact isolated CL-D1, CL-D2, or current condition actually shown. Consent is
`consented | declined | withdrawn`;
recording is `not-requested | consented | declined | withdrawn`; phase status is
`scheduled | started | completed | excluded | withdrawn`; exclusion
is `included | excluded | pending`. Mirrored consent, withdrawal, and deletion
values must match the shared ledger at closure.

`consumer-language-task-observations-v2` is one independently scored condition
row. Its primary key is `task_observation_id`; participant/session,
`comparison_pair_id`, assigned direction, task, locked state/condition,
exposure position/order, isolated artifact/source prototype and both manifest
hashes, copy variant, access strategy, exposure/score-lock times, scoring/
exclusion status, action/timing/turn/clarification measures, teach-backs,
security interruption, and opaque note code are all required. Valid paired
conditions share one comparison ID and have positions 1/2; candidate-only rows
use `n/a` and position 1. Candidate eligibility is exactly nine valid candidate
rows plus three attempted current-control rows per included person. Each valid
control may enter its separate control/paired aggregate. An excluded control is
omitted; its candidate survives only when that candidate score was already
locked before the control-local failure. The task schema never treats a
participant/status row as consent proof by itself.

`consumer-language-exposures-v2` maps each observed condition to exact isolated
bytes plus its source prototype/manifest coordinates. Pilot rows may use
`prototype_manifest_* = n/a` but must hash exact working bytes; formal rows must
join the frozen v1 source manifest. In formal rows,
`prototype_manifest_sha256` is the external SHA-256 of the complete validated
v1 manifest file, not its legacy omitted-self `manifest_sha256` cell. Every
exposure file is self-contained,
strict UTF-8/NFC/LF/single-final-LF HTML with no subresource request and only
its assigned direction/state/condition in response bytes, visible DOM, and
accessibility tree. `consumer-language-issues-v2` joins every issue/retest to an
exact task-condition row and rejects orphan or cross-round lineage.

No finding may be attributed to a direction without all joins. CL-D3 is
`rejected-untested`; `current` is diagnostic only. Neither is a valid aggregate
decision ID in v2.

## 12. Plan 005 open sort and candidate formation

### Open-sort cards

Use these 24 learner-goal cards. They intentionally omit route IDs and current
navigation labels:

1. Work out what this site is and whether to trust it.
2. Find out whether this site covers my exam.
3. Choose an exam when I am not sure which one applies.
4. Confirm an exam profile and see what is still unknown.
5. Start a short practice or continue a saved one.
6. Answer a question and find out why the answer is right.
7. Learn what a tool is used for and how to recognize it.
8. Compare tools that are easy to mix up.
9. Learn how a cleaning task is done.
10. Learn basic repair work expected of an entry-level custodian.
11. Practise spotting unsafe details in a workplace.
12. Go back over things I got wrong.
13. Set up a longer practice run and review the result.
14. Print a study packet for paper use.
15. Get a straight answer to a common exam or site question.
16. Check why an answer is supported and who is behind the site.
17. Report a correction or security concern.
18. Change reading or data preferences, export data, or preview a reset.
19. Make study material available without an internet connection.
20. Recover when a page or content is unavailable.
21. Understand why practice accuracy is not an official score.
22. Find out whether real exam questions can lawfully be provided.
23. Find out who runs and funds the independent site.
24. Tell New York City custodian exams apart from the statewide entry-level series.

The list number maps exactly to `OS-C01` through `OS-C24`. Compute
`card_set_sha256` from strict UTF-8/LF bytes in ascending card-ID order, one
line per card: `<card_id>\t<exact packet card text>\n`. Compute
`presented_order_sha256` from strict UTF-8/LF bytes in ascending presented
position 1–24, one line per position:
`<unpadded-base-10-position>\t<card_id>\n`. Neither preimage has a header, BOM,
blank line, trailing whitespace, or extra terminal LF.

For study ID number `p`, rotate the card stack by `(p - 1) mod 24`; reverse it
for even IDs. The participant groups and labels cards, identifies the first
place they would go, identifies what belongs everywhere, identifies what should
disappear in a question/hazard task, and places profile, offline, settings,
sources, and correction goals. Do not show the current header, candidate IA,
Plan 004 directions, route IDs, or internal vocabulary first.

Aggregate co-occurrence counts, repeated labels, disputed placements, mobile
expectations, player-exit expectations, and two materially different grouping/
priority models. Raw arrangements and quotations stay private. A candidate
difference must alter hierarchy/grouping, not only wording.

### Private open-sort evidence and candidate formation

The exact private schemas are `navigation-open-sort-sessions-v1`,
`navigation-open-sort-groups-v1`,
`navigation-open-sort-placements-v1`,
`navigation-open-sort-expectations-v1`, and
`navigation-open-sort-coding-v1` in the schema contract. Their files are,
respectively, `open-sort-sessions.v1.tsv`, `open-sort-groups.v1.tsv`,
`open-sort-placements.v1.tsv`, `open-sort-expectations.v1.tsv`, and
`open-sort-coding.v1.tsv`. Every session joins one eligible same-person/study
`research-consent-affirmed` event, the external
`fieldwork-phase-input-manifest-v1` SHA, exact protocol/card-set/order hashes,
and `candidate_exposure = none`.

Each eligible session must place each of `OS-C01` through `OS-C24` exactly once
at presented positions 1–24 with valid group joins and the packet's exact
rotation/reversal hash. Free text stays in access-controlled raw objects; TSV
rows carry raw-object SHA-256 plus the normalized value. Under pinned Node
22.22.0, normalize with Unicode NFKC, trim Unicode whitespace, collapse each
internal whitespace run to one ASCII space, and apply
`toLocaleLowerCase("en-US")`. Reject tabs, CR/LF, NUL, invalid UTF-8, or a raw-
object hash mismatch rather than silently rewriting them.

Coding is permitted only for `response_kind = free-text`. Every
`navigation-open-sort-coding-v1` row repeats `source_response_kind = free-text`
and its `source_text_sha256` must join the exact expectation row's
`response_raw_object_sha256`; a card/group/none response or changed raw hash
fails before independent coding or adjudication.

The exact expectation IDs are `arrival-first`, `global-everywhere`,
`player-hidden`, `profile-location`, `offline-location`, `settings-location`,
`sources-location`, `correction-location`, `mobile-menu`, and `player-exit`.
A coded free-text theme counts only after two distinct named human coders create
independent rows and a named human adjudicator emits the final code set. Final
rows bind the raw-text/codebook hashes and SHA-256 of the lexically sorted
complete independent-row set. Multi-label results are one row per code; only
`coding_stage = adjudicated` plus `decision = present` contributes.
The row-set digest is over canonical `navigation-open-sort-coding-v1` TSV bytes:
exact header plus only that response's independent rows, both set/adjudication
fields fixed to `n/a`, schema sort order with primary-key tie-break, strict
UTF-8/LF, and one final LF. It never hashes an adjudicated row that points back
to the set.

Derive open-sort aggregates deterministically from the eligible ID projection:

1. emit all 276 lexically ordered unordered card pairs with same-group
   numerator and eligible-session denominator;
2. count exact normalized labels and adjudicated codes;
3. count expectation responses by exact kind/reference;
4. sort pair rows by card IDs and remaining rows by the schema sort keys; and
5. emit exact `navigation-open-sort-aggregate-v1`: input/attrition/eligible-set
   coordinates, eligible study IDs/count, all 276 pair rows, normalized-label
   rows, adjudicated-code rows, expectation rows, and exclusion rows; then
   recursively key-sort JSON, encode strict UTF-8 with one LF, and hash the
   external file bytes.

Candidate formation is a traceable human synthesis, not a claimed clustering
oracle. `navigation-candidate-formation-v1` requires phase-input, eligible-set,
open-sort aggregate, route/task inventory, builder/version, template,
provisional-label, and codebook hashes; two exact models; a row for every
group/tier/order judgment with exact route/evidence-reference arrays; author,
distinct second reviewer, date, limitations; both hierarchy hashes; and
material-difference rows. The deterministic builder reproduces normalized
hierarchies from immutable inventory/template/label inputs and decision rows.
Prototype artifact bytes remain independently hashed; the record does not claim
that hashes alone recreate them.

The formation JSON has exactly two candidate objects with `candidateId`,
`decisionRowSetSha256`, `normalizedHierarchySha256`, and
`artifactVersionSha256`, plus exactly 42 decision rows: 21 frozen inventory
families × A/B. Each decision row has only `decisionId`, `candidateId`,
`familyNumber`, lexical unique `routeIds`, `viewId`, `parentViewId`,
`navigationTier`, `tierOrder`, `taskPriority`, lexical unique
`evidenceReferenceIds`, and `rationaleCode`. The builder lexically sorts by
candidate/family and derives the normalized hierarchy. Human judgment remains
visible in evidence references and rationale.

Each `decisionRowSetSha256` is over a recursively key-sorted UTF-8/LF JSON
wrapper with schema `navigation-candidate-decision-row-set-v1`, packet protocol,
candidate ID, and that candidate's exactly 21 decision rows sorted by numeric
family then decision ID. The wrapper contains no digest field.

The hash order is acyclic: raw files/artifacts → external phase-input-manifest
SHA → attrition/eligible projection → canonical open-sort aggregate SHA →
canonical candidate-formation file SHA → final phase-evidence manifest → owner
approval bound to that final manifest's external SHA. No formation or manifest
file contains its own hash. Git receives only safe aggregate coordinates.

## 13. Plan 005 pilot, tree test, and first-click rounds

### Locked task destinations and scenarios

The 13 formal task IDs remain unchanged. Candidate-specific first-click targets
and labels are locked only after the open sort and threshold pilot, but the
destination intent is fixed:

The moderator reads each prompt verbatim. Candidate-specific destination and
first-click mappings are then locked during the pilot without changing the
prompt.

| Task ID | Exact prompt | Destination intent |
|---|---|---|
| `profile-fit` | “A public notice says New York Entry-Level Custodians and Janitors. Show where you would check whether this site covers that notice.” | Published exam/profile chooser and truthful profile fit |
| `quick-practice` | “You want to answer some practice questions now. Show where you would start.” | Available practice start without assuming an official length |
| `review-misses` | “You want to go back over things you missed earlier. Show where you would do that.” | Local review queue |
| `tool-lookup` | “You want to recognize a pipe wrench and learn what it is used for. Show where you would look.” | Accepted tool reference |
| `tool-comparison` | “You keep mixing up a pipe wrench and an adjustable wrench. Show where you would compare them.” | Accepted tool-family comparison |
| `hazard-practice` | “You want to practise finding unsafe details in a workplace, using pictures or the text version you normally use. Show where you would start.” | Visual or nonvisual hazard practice |
| `simulation` | “You want a longer site-designed practice run. Show where you would set it up.” | Practice-simulation setup, not an official test claim |
| `print` | “You want a study packet you can use on paper. Show where you would make it.” | Print center |
| `offline-download` | “You expect to study without internet later. Show where you would prepare the material now.” | Compatible offline content |
| `settings-data-control` | “You want to save a copy of your study data and see what a reset would remove, without resetting anything. Show where you would go.” | Export and reset preview |
| `source-support` | “You want to check the public document behind an answer. Show where you would look.” | Source/transparency support |
| `correction-report` | “You found a possible mistake and want to see how to report it without sharing personal or exam material. Show where you would go.” | Correction-report guidance/form boundary |
| `unavailable-recovery` | “The study page you wanted is unavailable. Show what you would use to get back to something that works.” | Truthful status/recovery path, not an empty state |

`navigation-task-registry-v1` is the immutable 13-row source for task ID,
canonical position, priority, prompt hash, destination-intent code, both timeout
values, and both success codes. Priority membership is fixed as follows:
`top = profile-fit, quick-practice, review-misses, hazard-practice`;
`supporting = tool-lookup, tool-comparison, simulation`;
`utility = print, offline-download, settings-data-control`; and
`trust-recovery = source-support, correction-report, unavailable-recovery`.

The threshold pilot runs both low-fidelity candidate trees and representative
page starts with 2–3 pilot-only participants. They are eligible for the pilot
aggregate but excluded from every formal sample. Freeze
`navigation-threshold-pilot-schedule-v1` before the first `TP-P*` ID. It has
three participant slots (two primary and one reserve), each with exactly 16
trials: every candidate × method × priority cell once. Odd slots run candidate
A then B and tree then first-click; even slots reverse both block orders. Within
each priority list above, choose the trial task at zero-based index
`(participant_slot - 1 + candidate_index + 2 * method_index) mod list_length`,
where A/tree are index 0 and B/first-click index 1. Presentation index is the
resulting candidate-block, method-block, priority order (`top`, `supporting`,
`utility`, `trust-recovery`). Reset to the exact locked start state before every
trial, including block changes, and give no correctness feedback.

This representative schedule deliberately does not require 52 trials per
person. It covers every candidate/method/priority cell from at least two unique
people without cherry-picking after outcomes. Use the third precommitted slot if
one primary session is invalid; never reuse its study ID. Fewer than two valid
unique people in any cell stops the pilot. If 16 trials plus consent/debrief
cannot fit the disclosed 60 minutes without rushing, revise the participant-
facing duration, version the schedule/protocol, and rerun the threshold pilot
before formal IDs. Measure ambiguity, rescue, direct/indirect paths, and
plausible success ranges, then repair task wording and declare the eight numeric
method-by-priority thresholds. The declaration file locks task wording, scoring,
priorities, denominators, exclusions, critical definitions, and rounding.

Use exact private `navigation-pilot-artifact-manifest-v1`,
`navigation-threshold-pilot-sessions-v1`, and
`navigation-threshold-pilot-trials-v1` schemas. The artifact manifest is one
immutable candidate/method/task exposure row with exact entry-state and artifact/
hierarchy hashes; tree rows use `n/a` entry fields, while first-click rows bind
their exact start. Every session joins the shared
consent event, external phase-input manifest, exact protocol, task registry,
pilot artifact manifest, schedule file hash, and participant slot. Every trial
joins its exact schedule cell and artifact ID, candidate artifact/hierarchy, prompt/task/
priority, entry state, expected and observed destination/action, branch/
backtrack counts, timing, rescue/timeout, and valid/excluded disposition. Tree
and first-click successes are derived from observations; there is no trusted
entered outcome. Pilot people are excluded from every formal phase by
`program_person_key`.

First emit canonical `navigation-threshold-pilot-aggregate-v1`: input,
attrition, eligible-study-set, eligible-trial-set, and schedule coordinates;
the eligible trial digest is SHA-256 of lexical valid `trial_id` values, one
UTF-8 ID plus LF per line. Then include per-person coverage rows;
exactly eight rational metric rows with unique-person counts and per-person
min/max; exclusions; and the measured session-length review. That review is one
exact object with `declaredDurationSeconds`, `eligibleSessionCount`,
`minimumDurationSeconds`, `medianDurationNumeratorSeconds`,
`medianDurationDenominator`, `maximumDurationSeconds`, `noRushingObserved`,
`fitsDeclaredDuration`, `rerunRequired`, `reviewerCode`, and `reviewedAtUtc`.
Derive durations as `ended_at_utc - started_at_utc` for eligible sessions,
sorted ascending; min/max are endpoints and median is the middle value over 1
for odd `n` or the sum of the two middle values over 2 for even `n`.
`fitsDeclaredDuration` is true exactly when every duration is no greater than
the disclosed duration. `rerunRequired` is true exactly when duration does not
fit or the named human reviewer records `noRushingObserved = false`; a true
value blocks declarations/formal activation and requires a new versioned pilot.
Then close the
pilot with exactly eight rows in
`navigation-threshold-declarations-v1`: tree-direct and first-click for each of
`top`, `supporting`, `utility`, and `trust-recovery`. Derive pooled numerators,
denominators, and per-person min/max rational rates from valid trials only.
Tree success is `direct`; first-click success is `correct-first-click`.
Thresholds remain an explicit owner judgment, stored as exact numerator/
denominator rationals and bound to the external pilot-input-manifest and
aggregate-file hashes plus exact rationale. Display six-decimal half-up values
only after derivation and compare thresholds by integer cross-multiplication.
Each declaration `metric_id` must be the contract's exact metric for its
method/priority cell; denominators and threshold denominators are positive,
numerators are bounded by their denominators, and all pilot rational fields
byte-match the aggregate.
The declarations contain no self/set hash; the final phase manifest binds the
declaration file's external SHA. All rows predate every formal ID.

The private `fieldwork-phase-evidence-manifest-v1` closes schema, raw-file and
row-count hashes, input manifest, attrition projection, eligible trial set,
protocol/tasks, pilot artifacts/schedule, aggregate, and declaration hashes
without containing its own hash. A changed task,
hierarchy, artifact, scoring rule, exclusion rule, or threshold invalidates the
closure and requires a new excluded threshold pilot; it cannot be patched after
formal results exist.

### Tree-test counterbalancing

Start with the 13 task IDs in the order above and number their canonical
positions `t = 1..13`. For participant number `p`, rotate by `(p - 1) mod 13`;
reverse the presentation order for even `p`. Before the first formal ID, choose
one exact planned effective `n` from 15–20 and freeze a private
`navigation-phase-allocation-v1` schedule. Its CSPRNG-shuffled primary A/B counts
differ by at most one; each primary has `reserve-1` and `reserve-2` rows carrying
the same candidate, rotation, reversal, and task-order hash. Separately freeze
`task_order_sha256` as SHA-256 of strict UTF-8/LF bytes in final presentation
order, one `task_id` plus LF per line after rotation/reversal. Separately freeze
the exact 26-row `navigation-tree-schedule-v1` (both candidates × all 13 tasks)
with registry/prompt/priority, threshold-declaration file, artifact/hierarchy,
and expected-destination joins. Neither file contains its own hash.

After consent, assign the next unused primary. An excluded/withdrawn/invalid
session consumes its row and ID; the next new person/new ID receives that
primary's next same-candidate reserve. Never switch candidates to repair a
balance or use outcomes to choose a reserve. Exhausting a cell's reserves stops
the round and requires a new approved version/cohort. Each person uses one
candidate for all 13 tasks and never sees the alternate hierarchy. Reset the
tree with no answer feedback between tasks. The included A/B counts therefore
remain the precommitted balance and every candidate/task denominator equals its
included assigned people. The tree cohort is disjoint from open sort, threshold
pilot, and both first-click rounds.

Tree timeout: 90 seconds. Score:

- `direct` — reaches the locked destination without entering a wrong branch;
- `indirect` — reaches it without rescue after one or more wrong branches or
  backtracks; or
- `failed` — does not reach it by timeout, requests rescue, or reaches a wrong
  terminal destination.

### First-click R1 counterbalancing

Choose an exact effective `n` from 5–8 before the first R1 ID. Use the same task
rotation/reversal and a separately seeded frozen
`navigation-phase-allocation-v1` schedule whose primary A/B counts differ by at
most one and give each primary two same-candidate reserves. Apply the same
new-person/new-ID, outcome-blind, never-reuse, reserve-exhaustion stop rule as
tree testing. Each fresh included participant uses one candidate for all 13
tasks, so every candidate/task cell has at least two observations at the minimum
sample. Reload the exact start state between tasks, clear session state, give no
correctness feedback, and never show or teach the alternate model.

The entry-page template is complete and identical by candidate so entry context
does not confound A/B. It expands to 26 rows in
`navigation-first-click-schedule-v1`. Before formal exposure each row must add
the exact candidate artifact/hierarchy hash, entry-state hash, and candidate-
specific expected first-action ID; missing placeholders fail the formal gate.

| Task ID | Candidate A entry page/state | Candidate B entry page/state |
|---|---|---|
| `profile-fit` | `index.html` / `default` | `index.html` / `default` |
| `quick-practice` | `profile.html` / `default` | `profile.html` / `default` |
| `review-misses` | `practice.html` / `default` | `practice.html` / `default` |
| `tool-lookup` | `practice.html` / `default` | `practice.html` / `default` |
| `tool-comparison` | `atlas.html` / `default` | `atlas.html` / `default` |
| `hazard-practice` | `atlas.html` / `default` | `atlas.html` / `default` |
| `simulation` | `review.html` / `default` | `review.html` / `default` |
| `print` | `atlas.html` / `default` | `atlas.html` / `default` |
| `offline-download` | `profile.html` / `default` | `profile.html` / `default` |
| `settings-data-control` | `review.html` / `default` | `review.html` / `default` |
| `source-support` | `player.html` / `default` | `player.html` / `default` |
| `correction-report` | `utility.html` / `default` | `utility.html` / `default` |
| `unavailable-recovery` | `utility.html` / `unavailable` | `utility.html` / `unavailable` |

The `unavailable` row requires an immutable unavailable-state artifact. If
`utility.html` cannot expose that state without changing bytes, version and
rehash the candidate before approval; never simulate it in the session.

First-click timeout: 30 seconds. `correct-first-click` means the first
actionable destination chosen matches the predeclared target. No action,
facilitator rescue, browser chrome, or a wrong destination is
`incorrect-first-click`. Record the full eventual path separately; it cannot
change the first-click score.

After matrix-backed tree and R1 results, progress at most one candidate under
the section 15 gates. Freeze its evidence commit before Plan 004 vocabulary is
incorporated.

### First-click R2

Only after Plan 004 is accepted on `main`, copy the progressed candidate into a
new R2 scratch directory, replace provisional labels with the accepted content
contract, and prove that the normalized hierarchy is byte-for-byte unchanged
at the hierarchy-signature level. Obtain a new exposure approval. Every new R2
participant tests all 13 tasks on this one reconciled artifact. Rotate task
order as above. Project the progressed candidate's 13 schedule rows into a new
R2 schedule with the same entry page/state template and new exact artifact,
state, content-contract, and expected-action hashes. Obtain a separate R2
schedule/exposure approval before any `F2-P*` ID.

Choose exact R2 `n` from 5–8 and freeze its one-candidate primary plus two-
reserve-per-primary `navigation-phase-allocation-v1` schedule before the first
R2 ID. A withdrawal/exclusion consumes the row and uses the next precommitted
same-position reserve with a new person/new ID; reserve exhaustion stops the
round. This preserves task-order balance without reusing a learned participant
or selecting replacements from outcomes.

Run participant sessions with keyboard, screen reader, text enlargement/reflow,
and no-JavaScript coverage across the recruited access-strategy sample. Test at
320 CSS pixels and real 400% zoom where supported. A blocked access method is a
failure and possible critical issue, not a missing value to average away.

Every formal tree/first-click observation is one
`navigation-task-evidence-v2` row with a unique task-evidence ID; same-person/
study consent event; input manifest, allocation file/slot, candidate, method,
task registry/prompt/priority, threshold-declaration file, artifact/hierarchy,
entry-schedule/content-contract, exact expected and observed destination/action,
branch/backtrack counts, timing, rescue/timeout, and derived disposition joins.
Tree `direct | indirect | failed` and first-click
`correct-first-click | incorrect-first-click` are computed from those raw
fields; no entered outcome can make a gate pass.

`navigation-issues-v1` joins each issue occurrence/family to that exact task row
and repeats candidate/artifact/hierarchy/registry/prompt coordinates. Critical
and high rows require a second named human review. A critical family remains
unresolved until a later eligible independently reviewed row for the same
candidate and unchanged hierarchy explicitly references the family, records
`retest_outcome = passed`, and `disposition = resolved`.
`navigation-formal-aggregate-v1` then contains cutoff/input/attrition/eligible
coordinates, exact per-candidate/task rational gate rows, issue lineage, unique-
person access coverage, and exclusions. `navigation-progression-decision-v1`
binds the tree and R1 aggregate files and is the only permissible source of a
selected R2 candidate.

## 14. Coding, severity, exclusions, and adjudication

### Plan 004 task anchors

Code teach-back as `accurate`, `partial`, or `incorrect` against these anchors:

| Task | `accurate` requires |
|---|---|
| Proposition | independent/unofficial original practice, intended audience, and a plausible first action |
| Profile fit | uses compatible public profile facts and preserves unknown/not-applicable states without guessing |
| Commitment | answer is editable before explicit submission; reveal follows successful durable commitment |
| Feedback/source | identifies outcome, rationale, and how to reach supporting public evidence |
| Hazard feedback | distinguishes a missed unsafe detail from a safe detail marked in error |
| Review | understands due/reviewed as local study workflow, not mastery or an official schedule |
| Offline failure | knows the prior usable version/data remain and what is currently unavailable |
| Import/reset | names the exact scope changed and retained before any destructive action |
| Advanced evidence | can find source/version detail without needing it in the default task path |

`partial` preserves the core consequence but misses a noncritical element;
`incorrect` states the wrong consequence, needs rescue, or cannot complete the
teach-back. Record first action, time to first action, wrong-turn code,
clarification count, repeated/replaced term codes, direction/order, and access
barrier separately.

### Severity

Critical—one occurrence is enough—includes:

- belief that the site is official or that a site result predicts an official
  score, pass, count, weighting, or readiness;
- confusion between editable/unsaved and committed/saved state or the scope of
  a destructive local-data action;
- precommit answer leakage or raw internal diagnostics;
- inability to reach required explanation/source content with the participant's
  access method;
- navigation that loses material profile context, requires JavaScript for a
  required public/reference path, traps a focused player, or creates a
  reasonable risk of unintended data loss; or
- any unresolved exam-security or privacy breach.

High: a noncritical task failure, materially wrong consequence, or access block
that requires facilitator rescue. Medium: an unaided recovery after a wrong
turn, repeated hesitation, or clarification that materially delays the task.
Low: a wording preference or brief hesitation with no wrong action or
misunderstood consequence.

For navigation, retain the failure taxonomy: wrong category, unclear label,
missed profile context, utility/trust detour, player-exit failure, compact-menu
failure, no-JavaScript failure, accessibility failure, and no action.

### Exclusions and protocol deviations

Exclude from scored denominators, while retaining aggregate counts and reason:

- consent incomplete;
- participant withdrawal;
- secure-material interruption that contaminates the task;
- wrong, changed, or unapproved artifact/order;
- technical failure that prevents the locked task;
- facilitator teaching/rescue before the score is recorded; or
- duplicate/ineligible participant.

Eligibility is derived at an explicit UTC phase-closure cutoff. Strict-parse
the shared append-only event ledger, follow `prior_event_id`, and apply the
latest effective event for each scope. A row contributes only when research
consent predates exposure, the session completed, no applicable later research-
consent revocation/participant withdrawal/deletion/researcher stop/exclusion
exists, and every
artifact/protocol/schedule join passes. `analysis_disposition` in a phase file
must equal this derivation; it cannot make itself eligible. Plan 005 formal rows
use `navigation-task-evidence-v2`, not the legacy four-column participant file.

A pre-closure withdrawal invalidates the phase evidence manifest and requires
deterministic re-aggregation before publication. It is not converted to an
ordinary `completed` or unexplained `excluded` row. Raw deletion follows the
approved scope; the event ledger retains only its opaque tombstone. Do not
delete or exclude an unfavorable valid session.

A protocol defect discovered after formal results are visible requires a new
versioned round; it never licenses a changed threshold or overwritten
candidate.

The moderator codes within 24 hours against the locked codebook. Every critical
and high issue is independently checked by a second authorized reviewer who
sees only the minimum de-identified excerpt/event code. Disagreement is resolved
against observed behavior, with an adjudication code; unresolved disagreement
uses the more severe code and is reported as a limitation. No invented reviewer
or agent persona may fill this role.

## 15. Decision thresholds

### Plan 004 direction eligibility

No percentage or population claim is made. A direction may be presented for
owner selection only when:

1. at least three completed participants saw it in each of R1 and R2;
2. every R1 critical issue for it was explicitly retested in R2;
3. it has zero unresolved critical issues in R2;
4. no high-severity issue repeats for two participants in R2 without an
   evidence-based revision and new retest;
5. every candidate-eligible participant has nine valid candidate rows and three
   independently attempted current controls joined through the v2 participant,
   attrition, allocation, exposure, prototype, task/state/variant, issue,
   withdrawal, and exclusion coordinates; a contaminated control is omitted
   from control/paired denominators, and its candidate row survives only when
   its independent score locked before the control-local failure;
6. candidate aggregates use candidate rows only; current controls and paired
   cross-tabs are reported separately by task and randomized order stratum;
7. every private input and derived aggregate hash matches the v2 evidence
   record, and preserved product/exam/security facts pass authority review; and
8. access-strategy failures and missing cohorts are reported rather than
   averaged away.

If neither CL-D1 nor CL-D2 is eligible, stop for a new versioned iteration. If
both are eligible, the owner may select one only with a manifest-bound rationale
that cites exact observations and limitations. Executor preference, desk-audit
severity, benchmark imitation, or a nonempty direction ID is insufficient.
CL-D3/current cannot enter this eligibility set under packet v2.

### Plan 005 threshold declaration and selection

The eight numeric thresholds remain genuinely unknown until the real 2–3
person threshold pilot. Immediately after that pilot and before formal IDs:

1. record observed direct tree and first-click ranges for each locked priority;
2. declare one tree-direct and one first-click threshold for each of `top`,
   `supporting`, `utility`, and `trust-recovery`;
3. explain why each is decision-useful but not a population estimate;
4. encode displayed rates to six decimal places using decimal half-up rounding;
   derive `thresholdMet` from the unrounded rational numerator/denominator; and
5. commit/push the values and never tune them after formal results are visible.

Regardless of numeric values:

- both A and B must have complete, valid 13-row tree and R1 first-click
  evidence before progression;
- an eligible progression candidate must meet all 13 predeclared tree gates,
  have zero unresolved selected-candidate tree criticals, meet all 13
  predeclared R1 first-click gates, and have zero unresolved selected-candidate
  R1 critical issues;
- derive the eligible-candidate set from the private matrices and reject a
  hand-entered `candidateProgression.firstClickRoundTwoCandidateId` outside it;
  if both qualify, the owner records an evidence-bound rationale; if neither
  qualifies, stop without progressing a less-bad model;
- the selected R2 candidate must meet all 13 predeclared R2 first-click gates,
  resolve every carried critical issue, retain its passing tree invariant, and
  satisfy the minimum four distinct access-strategy people across the program;
- no accessibility or no-JavaScript blocker can be averaged into a pass; and
- a locked hierarchy may not be revised under its old candidate/artifact ID.

Only then may the owner approve the exact tested candidate. The selection,
canonical-promotion approval, and four maintained product-contract changes must
all bind the same candidate, artifact, hierarchy, and accepted language hashes.

## 16. Incident and adverse-event handling

| Event | Immediate action | Evidence/data action |
|---|---|---|
| Remembered/secure exam content | Interrupt: “Let's stop there—I cannot collect or repeat that.” Move on or end | Do not record content; remove any accidental capture; retain only `security-interruption`; compensate fully |
| Accidental PII/document exposure | Stop viewing/recording, close the material, notify data custodian | Quarantine and delete the capture; retain only incident category/date; never commit it |
| Distress or fatigue | Pause, offer break/skip/stop, do not probe | Withdrawal/skip rules; compensate fully if stopped |
| Accessibility barrier | Stop forcing the inaccessible path; offer reschedule only after repair | Score the locked task as blocked/failed where valid; open critical review; do not substitute observer clicks |
| Technical failure | Allow one restart only when artifact/state/order remain exact | Otherwise invalidate technically, retain aggregate reason, and reschedule under a new ID |
| Facilitator conflict or leading | Pause; replace facilitator if possible | Exclude contaminated task/session and record protocol deviation |
| Participant withdrawal | Stop immediately, no reason required | Apply section 7 deletion and compensation rules |
| Suspected privacy/security incident | Stop collection and access to affected data | Notify named incident owner within 24 hours; preserve no prohibited content in the incident note |

The moderator does not confirm whether offered exam content is genuine, provide
medical/legal advice, or promise confidentiality beyond the approved storage
and incident process.

## 17. Analysis, synthesis, and promotion sequence

### Deterministic private-to-aggregate derivation

Use the schema contract's exact headers, primary keys, sort keys, and canonical
encoding. Hash every source file's exact validated bytes, derive the shared
attrition projection at the named UTC cutoff, sort eligible study/person keys,
and hash that set before reading outcomes. Unknown columns/enums, duplicate
keys, broken joins, noncanonical bytes, or a fixture ID fail closed.

Every phase uses one acyclic digest graph: validated raw files and immutable
artifacts → external SHA of `fieldwork-phase-input-manifest-v1` → cutoff
attrition projection and eligible-set hashes → external canonical aggregate
SHA → external human decision/declaration SHA when applicable →
`fieldwork-phase-evidence-manifest-v1` → owner approval bound to the final
manifest's external SHA. No file contains its own digest. An exposure artifact-
set digest alone uses the contract's sorted `artifact_sha256`, two spaces,
`artifact_id`, LF recipe; it is not a manifest-file hash.

For Plan 004, join participant → exact research-consent event/cutoff projection
→ frozen allocation file/occurrence → task condition → exposure/source manifests
→ issue. Require nine valid candidate and three attempted control rows per
candidate-eligible formal person and verify score-lock chronology. Candidate
aggregates are exact counts grouped by `phase, assigned_direction_id, task_id,
candidate_order, outcome`; valid controls are separately grouped by `phase,
task_id, current_order, outcome`; paired tables count exact
`candidate_outcome × current_outcome` cells. Candidate denominators never
include control rows, and an excluded control enters neither control nor paired
denominators. A candidate paired with an invalid control remains eligible only
when its score locked before the control-local failure; upstream contamination
excludes the candidate and triggers replacement. Derive issue/retest state by
exact issue lineage. Lexically sort every key, recursively key-sort JSON objects, preserve declared array
order, encode UTF-8/LF, and hash participants, attrition projection, allocation,
exposure/source manifests, task rows, issue rows, and aggregate bytes.

For Plan 005 open sort, use the 276-pair, label/code, and expectation derivation
in section 12. For the threshold pilot, derive rational success counts and
per-person ranges for all eight cells from valid trials before comparing the
hash-bound declarations. For formal tree/first-click, group valid
`navigation-task-evidence-v2` by exact phase/candidate/task. Tree numerator is
`direct`, with `indirect` and `failed` retained separately; first-click
numerator is `correct-first-click`. Denominator is their exact valid sum.
Derive unresolved criticals from joined issue/retest rows. Compare a rate to its
threshold by integer cross-multiplication; the six-decimal string is display
only. Recompute all task rows, the 13+13 R1 candidate gates, progression set,
selected R2 rows, artifact/hierarchy/schedule hashes, and phase evidence
manifests.

For both plans, access coverage is the set cardinality of distinct eligible
`program_person_key` values with an access-strategy-used event. Count each
person once overall and once per applicable strategy category; never sum
overlapping categories or phase seats. A timely withdrawal changes the
attrition projection, invalidates the unpublished phase manifest, and requires
full re-derivation. No hand-entered count, rate, `thresholdMet`, eligibility,
progression, or compatibility status is authoritative.

After each valid phase:

1. hash and verify all private sources without copying them into Git;
2. derive exact aggregate numerators/denominators, attrition/exclusion counts,
   and phase-manifest coordinates;
3. separate observation, inference, recommendation, owner decision, and
   unresolved fact;
4. report coarse segment/device/access coverage and missing cohorts without
   identifying combinations;
5. report contradictions and all valid negative evidence;
6. retain only safe aggregate findings, task/card labels, artifact hashes,
   limitations, and approval coordinates in Git; and
7. commit and push incrementally without force.

Plan 004 must reach its evidence and owner gate first. Only then may its
accepted rules enter `product/CONTENT_DESIGN.md` and only then may Plan 005
reconcile R2 labels. Plan 005 changes `product/ROUTES.md`,
`product/SCREEN_STATES.md`, `product/COMPONENT_ARCHITECTURE.md`, and
`product/DESIGN_SYSTEM.md` only after all formal thresholds and the exact owner
decision pass. Production UI code remains out of scope. `plans/README.md` may
say `DONE` only after the corresponding final verifier, product promotion,
validation, remote-head, and draft-PR gates pass.

## 18. Resume checklist

Apply only the checklist for the intended next action; do not require formal-
round facts to screen someone or conduct an approved exploratory phase.

### Before recruitment/screening

- [x] Step 1 is released at
      `9fc7dcacfc961752e5d9a2cedbc426deead54a05` and the durable recovery package
      is reconciled without changing truthful zeros.
- [ ] A real recruitment channel, contact owner, authority, availability
      window, and real target-user access are named.
- [ ] Exact paid or unpaid terms, funding/delivery where applicable, and
      cancellation terms are approved in all eight
      `fieldwork-compensation-terms-v1` phase rows.
- [ ] The contact/payment registry, custodian, access controls, retention,
      deletion, backup, and withdrawal process are approved.
- [ ] The access-separated research evidence store, screener writer, custodian,
      access controls, retention/deletion, and withdrawal process are approved
      and `fieldwork-screener-v1` plus shared attrition validation pass.
- [ ] The exact contact copy/screener/contact-consent materials and incident
      owner are instantiated.
- [ ] Owner-authored schema-amendment and operations/sample comments are present
      on both #37 and #38; these are not formal exposure approvals.

### Before an uncounted pilot, open sort, or threshold pilot

- [ ] The relevant section 2.2 phase row is ready.
- [ ] The separately controlled evidence store, custodian, access, Plan 004 and
      Plan 005 retention/deletion processes are approved.
- [ ] The named moderator, incident owner, and second human reviewer are
      available.
- [ ] Shared attrition and relevant phase schemas/validators pass; recording is
      explicitly false unless separately approved.
- [ ] The exact safe pilot/card/candidate bytes, protocol, schedule, device,
      browser/AT, private exposure boundary, and hashes match the phase.

### Before a formal round

- [ ] The relevant exploratory phase is valid and the post-pilot artifact,
      task/scoring contract, thresholds where applicable, and immutable
      schedules/manifests are frozen.
- [ ] The cohort-reuse check passes by `program_person_key`.
- [ ] Owner-authored structured schema/operations/sample/exposure gate comments
      resolve on #37/#38 and match exact hashes.
- [ ] The full formal verifier and tamper fixtures pass before any formal ID or
      link is issued.
- [ ] No secure exam material, identity, contact/payment data, raw note,
      credential, or private path is in Git.

Current state remains `NEEDS ATTENTION — external participant resources`.
No participant access, channel, payment/no-payment terms, two approved private
systems, named human team, session facilities, or owner gate comments were
available during this amendment. Full repository-operation approval supplied
authority to prepare and publish this packet; it did not supply those people,
funds, systems, facilities, or decisions.
