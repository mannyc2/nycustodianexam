# Expanded Hazard source receipts

Captured the first claim's three source receipts with all nested technical details expanded at 1042/384 CSS widths, in visual and written saved Review. All four captures completed with zero page errors and no document horizontal overflow. Source and hashes are in the manifest. Visual/written receipt captures are byte-identical at each width: False.

Inspected the complete visual-mode crops in overlapping readable-scale slices. No observed clipping or overlap; publisher, date, scope, excerpt and locator records remain present. Compact blockquotes have a narrow measure and long technical IDs wrap over multiple lines; these are readability limitations, not resolved design acceptance. Only this first claim's receipts were inspected, not every claim or historical source format.

The capture workflow now supports NYCUSTODIAN_HAZARD_CAPTURE_RECEIPTS=1 with an explicit output directory. The first temporary capture assumed one nested Receipt details summary and stopped on strict locator ambiguity; the final workflow opens all three. Log: /tmp/nyc-hazard-receipts-final.log.
