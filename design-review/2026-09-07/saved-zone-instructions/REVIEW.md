# Saved zone instruction correction

The visual and compact written Review captures at 1042/384 CSS widths were regenerated after the saved-instruction correction. Both fieldset crops were inspected at readable scale. The copy describes locked saved choices; the selected first zone remains distinct, and instructions, labels and descriptions wrap within the fieldset without observed clipping or overlap. No page errors or horizontal overflow occurred in the 16-case capture workflow. Only the two relevant saved-written full pages and inspected crops are retained here.

Source and SHA-256 values are in `manifest.json`. The full-page files provide context; this checkpoint inspects the changed fieldset rather than recertifying all long-page content. Open receipt disclosures, other scenes/states, desktop column layout and broader acceptance remain open. Log: `/tmp/nyc-saved-zone-final-capture.log`.
