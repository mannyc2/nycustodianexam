# Explorations — history only

Nothing in this folder is an implementation reference.

- `Site Nav Proposals.dc.html` — the three global-navigation structures and four active-state
  treatments explored before the settled navigation was chosen. The settled result (Practice,
  Library, Exams, Settings; Library as a disclosure; bottom tab bar below 48rem) is drawn in every
  page in `../current/`.

Earlier turns of `Landing.dc.html` and `Study Hub.dc.html` live inside those files, below the
settled design, behind a badge reading `Turn N — archived …` and a heading starting `Archive —`.
They predate the no-active-exam decision and still show exam chips and pinned-exam copy. Do not
build from them.
