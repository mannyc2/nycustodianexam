# Source quotation reading width

Removed default inline blockquote margins inside player source disclosures. Expanded receipt captures were generated against the manifest base commit plus the recorded CSS file hash. All four visual/written captures at 1042/384 CSS widths completed without page errors or document overflow. The first 1300 pixels of each visual-mode receipt crop were inspected, covering the first complete receipt and the following receipt header.

At compact width, the first quotation now uses the card text width rather than an unnecessarily indented narrow column. No observed clipping or overlap in the inspected region. Earlier screenshots remain in ../hazard-expanded-receipts for comparison. Long technical IDs still wrap; this change does not certify all receipt content, historical variants or accessibility. Production build/artifact checks passed. Logs: /tmp/nyc-receipt-measure-build.log and /tmp/nyc-receipt-measure-capture.log.
