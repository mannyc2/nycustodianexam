# Source citation browser evidence

Working-tree source hashes and base commit are in manifest.json. Chromium captures show the source detail page at 1042px and 384px, with the first technical disclosure expanded. Both full images were inspected in overlapping readable crops, including the footer. Metadata and IDs wrap without horizontal overflow. Compact excerpts retain the browser's blockquote indentation, making their reading measure narrow; this remains a presentation improvement to resolve. The fixed bottom navigation appears within the full-page compact screenshot and obscures one captured strip, so that strip is not visually certified by this image. These are implementation observations, not a completed comparison against an accepted source-page reference.

All six desktop/compact browser cases passed across Chromium, Firefox and WebKit: metadata, claim disclosure, horizontal reflow, offline link disabling, continued disabling after reconnect, and restoration on fresh reload. No unexpected page errors occurred. The existing cached profile/source test also passed in Chromium; its other two engines are explicitly skipped. Its Supported scope assertion now locates the named definition instead of relying on an obsolete row index. Browser TypeScript passed. Log: /tmp/nyc-source-citation-browser.log (7 passed, 2 skipped).
